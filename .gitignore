/target
**/*.rs.bk
.DS_Store

# dev harness runtime artifacts
/dev/.kubeconfig
/dev/server.pid
/dev/server.log
# local object-store backend (default dev fallback)
/.scarab

# UI (TS project, not a cargo member)
/ui/node_modules
