[package]
name = "scarab-forge-github"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
scarab-forge = { workspace = true }
async-trait = { workspace = true }
reqwest = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
hmac = { workspace = true }
sha2 = { workspace = true }
jsonwebtoken = { workspace = true }
tokio = { workspace = true }

[dev-dependencies]
rsa = { workspace = true }
# The stub GitHub that `tests/app_preflight.rs` serves over a real socket. The
# forge is a true external (ADR-0017), so it is a real HTTP server rather than
# a mock object — that is the only way to see the JWT the adapter actually put
# on the wire.
axum = { workspace = true }
