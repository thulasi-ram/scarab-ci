{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774735,"nonce":"c+DLahM7YvFb4tAxbmE8jpWBWiI=","title":"ci+api+helm: gate UI vitest, tag the OpenAPI spec at source, first-class forgejoWebhookSecret","message":"\u003e Imported from pull request #73 (MERGED)\n\u003e Branch: `chore/ci-and-chart-gaps` → `main`\n\u003e Created: 2026-07-25T16:30:11Z · Merged: 2026-07-25T17:30:12Z\n\nThree small open tickets from the sweep, each independently verifiable.\n\n`ci.yml` ran typecheck + generated-client drift for the UI but never the\nno-DOM derivation suite, so the 72 tests over `takes` / `dag-layout` /\n`attempts` / `events` / `logs` shipped unguarded. Adds a `ui-unit` job\nrunning `npm test` as a sibling of `openapi-drift`, so a red derivation\nreads differently from a stale contract. Coverage stays report-only per\nthe test strategy.\n\n`starlight-openapi` builds per-operation pages and sidebar groups from\ntags; the untagged spec published as a bare overview with **no operation\npages at all**, and the docs site worked around it by deriving tags into a\ngitignored copy.\n\nTags now live in the canonical spec: a `TagGroups` utoipa modifier stamps\neach operation with a curated group derived from its path and declares the\ngroups top-level, with descriptions, in display order. One modifier rather\nthan 53 `#[utoipa::path]` edits — the grouping is a single readable list,\nand a new route inherits a group instead of silently landing untagged.\n\n`operation_tags_cover_every_operation` is the gate: exactly one declared\ngroup per operation, no empty groups, and a path no rule claims fails the\nbuild. The docs indirection is then deleted (`scripts/gen-openapi.mjs`\ngone, `astro.config.mjs` points back at `../../openapi.json`).\n\nResulting groups — 53 operations, none untagged:\n\n| Runs | Auth \u0026 Identity | Repositories | Environments | Secrets | System | Webhooks |\n|---|---|---|---|---|---|---|\n| 23 | 11 | 5 | 5 | 4 | 3 | 2 |\n\nThe chart rendered `SCARAB_GITHUB_WEBHOOK_SECRET` from a value but had no\nequivalent for Forgejo, even though the Forgejo adapter and\n`/webhooks/forgejo` ship (ADR-0046) — operators had to fall back to\n`existingSecret` / `envFrom`. Adds `secrets.forgejoWebhookSecret`.\n\n- `cargo test -p scarab-server` — 188 passed, 1 ignored\n- `npm test` in `ui/scarab-web-ui` — 72 passed\n- `npm run build` in `ui/scarab-docs-ui` — 133 pages, **61** under\n  `reference/api` (all 53 operations get a page)\n- `helm template` renders both webhook-secret keys\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}