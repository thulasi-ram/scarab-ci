{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774726,"nonce":"VO/yQK0dQrUS7icTur9vb/90Lf0=","title":"ADR-0058: runtime service containers (Sidecar + Shared)","message":"\u003e Imported from pull request #51 (MERGED)\n\u003e Branch: `feat/adr-0058-services` → `main`\n\u003e Created: 2026-07-21T11:16:02Z · Merged: 2026-07-21T12:39:44Z\n\nImplements **ADR-0058** — runtime backing containers for pipelines (the Woodpecker/GHA \"services\" need), adapted to Scarab's Pod-per-Step topology. Designed in a grill-with-docs session; two forms, **neither a `needs`-able DAG node**.\n\n\n| Slice | git-bug | What |\n|---|---|---|\n| **1 — Sidecar service** | `f88a1ab` | Default form: a `services:` field on a Step → container co-located in that Step's Pod (native-sidecar pattern, ADR-0042), reachable at `localhost:\u003cport\u003e`, `ready:` probe gates the main container. Fenced by inheritance. |\n| **2 — Shared service** | `58646c1` | Opt-in form: pipeline-level `services:` + per-Step `uses:` → durable Run-scoped resource keyed `{run, take}`, eager birth, standalone Pod + k8s `Service` + `NetworkPolicy` scoped to opt-in Pods, scheduler readiness-gate, fresh instance per Take. |\n| **3 — Failure semantics** | `5505de8` | Startup flake → bounded auto-retry (readiness budget is the bound); mid-run death → fail-closed, opt-in steps fail + cascade (ADR-0027), **no auto-rerun / no Take fork**; non-opt-in steps unaffected. |\n| **4 — Evidence** | `13f6555` | `GET /v1/runs/{id}/services` + `.../services/{service}/logs` (SSE, best-effort ADR-0013 tail); UI **Services panel beside the DAG** (not a graph node). |\n\n- `just check` green; full `just test` green (104 test sections, 0 failures).\n- UI `tsc --noEmit` clean + `vite build` succeeds; `openapi.json` + UI schema regenerated.\n- All coverage is in-process (testkit fakes / spec assertions) — **not yet live-verified on a cluster** (consistent with the ADR posture).\n\n- **Docs corrected**: `scarab-executor-local` is a host-process spawner (not a kind cluster) → it *rejects* container-image services; the k8s executor (run against kind/colima) is the service path. ADR-0058 consequence updated to match.\n- **Per-Take isolation** is enforced at the engine layer (`{run, take}` key); the k8s executor uses a fixed namespace, so true per-run-namespace object isolation awaits that substrate.\n- **Governed grants** (`add-capabilities`/`privileged`) keyed on the *service* image digest are deferred (fail-closed); only self-service `run-as-root` carries to service containers.\n- **Sidecar-log fold** under host-Step logs deferred → follow-up `c0c5e0a` (needs a StepPane restyle, out of slice-4 scope).\n\nDesign: `docs/adr/0058-runtime-service-containers.md`; glossary in `CONTEXT.md` §4.1.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}