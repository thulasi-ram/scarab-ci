{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774722,"nonce":"GZAyBYbAsay68TkSyR6llVif77k=","title":"fix(web-ui): run-detail \u0026 run-pipeline UI polish + workspace-tab crash fix","message":"\u003e Imported from pull request #42 (MERGED)\n\u003e Branch: `fix/run-detail-ui-polish` → `main`\n\u003e Created: 2026-07-20T06:10:17Z · Merged: 2026-07-20T06:11:54Z\n\n\nA batch of Run-detail / Run-pipeline polish from a UI review session. All changes are client-side (`ui/scarab-web-ui`), `tsc --noEmit` clean, and spot-checked in the browser against the local proc stack.\n\n- **Activity panel** — dropped the card surface; lays flat on the page background (it's a log, not a block).\n- **Artifacts panel** — same flat treatment for a consistent run-level footer with Activity; empty state + rows aligned to the page margin. `.rd-grid` gained a bottom margin so Artifacts no longer butts against the DAG/step grid.\n- **Run-pipeline ref picker** — resolved commit SHA moved below the input instead of beside the button.\n- **Secrets** — inline NAME/value inputs replaced by a **New-secret modal** with an **overwrite guard**: Save stays disabled until \"overwrite\" is ticked for an already-existing name (the API is an unconditional upsert, so this prevents accidental clobber).\n- **Logs empty state** (`no output`) — now renders with the line-number gutter instead of hugging the margin.\n- **Workspace tab crash** (the main bug) — a failed browse used to re-throw during render and tear down the whole step pane (tabs and all). `listWorkspace` and `fileBody` now swallow errors into a graceful empty/\"could not read\" state, and the no-snapshot state offers **\"Explore in a Debug pod →\"** (with a note that a debug pod is a *reproduction*, not the attempt's immutable bytes).\n\n- `docs/handoffs/take-attempt-ergonomics.md` — an unfinished design brainstorm on how to explain the run-history model (Take vs attempt) to end users, to be continued in a fresh session.\n\n- git-bug **`4c27eac`** — searchable branch/tag ref picker (needs a new `GET .../refs` endpoint) + restoring Runs-list filters by PR/user/branch.\n\n- `npm run typecheck` clean.\n- Verified in-browser: Activity/Artifacts flat treatment, ref-SHA placement, secrets modal + overwrite guard (created/guarded/deleted a test secret), workspace-tab graceful degrade. Not run through a full pipeline execution (UI-only changes).\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}