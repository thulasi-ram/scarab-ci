{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774725,"nonce":"13uCDtVJ1uaEkBwV2N0UDEvuAsI=","title":"feat(web-ui): gold emblem in both themes + motion polish","message":"\u003e Imported from pull request #50 (MERGED)\n\u003e Branch: `feat/ui-gold-logo-motion` → `main`\n\u003e Created: 2026-07-21T10:31:41Z · Merged: 2026-07-21T10:51:07Z\n\n\nA small brand + motion-polish pass on the web UI.\n\n- Use the **gold scarab emblem in light theme too** (previously ink-outline on light, gold only on dark). Drops the theme-conditional — one emblem import, gold carapace in both themes.\n\n- Add a strong custom `--ease-out` curve; the built-in CSS `ease` is too weak to read as intentional.\n- `:active` **press-feedback** (subtle `scale`) on the pressable controls — buttons, brand, search chip, theme toggle, repo cards — so the UI confirms it heard the press. Guarded under `prefers-reduced-motion`: colour/border feedback stays, the press-scale movement drops.\n- Smooth two hovers that were snapping with no transition — the search chip and the breadcrumb (`crumb-head`) link.\n\n- Page nav tabs now emphasise with **font-weight**, not just colour. A hidden bold ghost (`::after`, keyed on `data-label`) reserves the heavier width so the active-weight change **never nudges neighbouring tabs**.\n- This also fixes a **pre-existing layout shift** on every tab switch (the active tab went to `600` and shoved its siblings right). Scoped to `[data-label]`, so the inspector tabs are untouched.\n\n- Verified live in the dev UI in both themes.\n- Tab shift measured before/after an active-tab switch: **max sibling movement 0px**.\n- `tsc --noEmit` clean.\n\n- Only `transform`/colour animate (GPU-friendly); all UI durations ≤120ms.\n- Branch cut fresh from `origin/main`.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}