{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774747,"nonce":"WpOY72xxqmKlW90mGf1XNisTmbA=","title":"The pack reclaimer: stale staging + crash orphans, never a committed pack (ad79c90)","message":"\u003e Imported from pull request #100 (MERGED)\n\u003e Branch: `feat/ad79c90-pack-reclaimer` → `main`\n\u003e Created: 2026-08-25T09:21:22Z · Merged: 2026-08-25T21:41:39Z\n\nThe pack reclaimer (git-bug `ad79c90`) — the first code that deletes pack data, so it went through the full chain: plan → skeptical audit → independent judge (**the audit caught a real silent-loss race in the plan before a line was written**) → build → suite → skeptical review. **892/892 green** (real PG + MinIO), including a constructed-interleaving race test.\n\n**What it reclaims (and the hard line):**\n- Uncommitted staging from failed/abandoned drains: rows first (one `FOR UPDATE` transaction, PG-`now()` cutoff at 2× the fence-token horizon), bytes on a later pass.\n- Crash orphans: pack objects with no row, older than the bound, deleted only on a replica's **second consecutive** rowless observation — byte grace survives N reclaimers and restarts (restart = longer grace, never shorter).\n- **Never a committed pack.** The stop-line comment marks the `ec294b7` gate; every destructive statement is keyed `NOT committed`.\n\n**Slice 0 (lands first, closes the audit's BLOCKS finding):** `ledger_append` now refreshes `written_at` on re-PUT (a live crash-resume re-drive was previously invisible to the quiet predicate), and the drain's durable-closure check re-runs *inside* the record transaction after the committed-flip — judge-verified airtight at READ COMMITTED, pinned by a test that constructs the gate-vs-reclaim window and fails on pre-branch code.\n\n**Multipart hygiene:** `discard_open` and graceful shutdown now abort open uploads; the `mc ilm` abort flag was verified **nonexistent** on the shipped image (MinIO natively expires stale uploads), so minio.yaml documents that fact and values.yaml documents the AWS `AbortIncompleteMultipartUpload` lifecycle rule as a deployment requirement.\n\nObservability: five counters + per-pass log lines; every fail-closed skip says why nothing was deleted. Worst documented failure mode is a 422 re-drive (re-upload) — never loss.\n\nFollow-up notes from review (LOW, not blocking): a timeout wrapper on the shutdown abort loop; FOR-UPDATE deadlock aborts land on fail-closed paths.\n\nCloses `ad79c90` on merge. `ec294b7` (cross-fence refcount) remains the gate before committed-pack expiry.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}