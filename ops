{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774724,"nonce":"TCFLbITGBdOimCRnxdXDKuvFIMo=","title":"feat(adr-0057): UUIDv7 run id + per-repo run number, A·2 gutter UI","message":"\u003e Imported from pull request #46 (MERGED)\n\u003e Branch: `feat/adr-0057-run-number` → `main`\n\u003e Created: 2026-07-20T22:30:40Z · Merged: 2026-07-20T22:54:48Z\n\nAmends **ADR-0057** and implements the run-identity split we designed.\n\n- **Internal run id → UUIDv7** (opaque, unguessable, lexically time-sortable) via a single `new_run_id()` helper — replaces the fully-random v4 that fragmented the `runs` `TEXT` primary key. Session/CSRF/state ids stay v4.\n- **Per-repo run number `#N`** — migration `0034` (`runs.run_number`, a `repo_run_counters` table, partial `UNIQUE(tenant_org, tenant_project, run_number)`), allocated atomically (`INSERT … ON CONFLICT … RETURNING`) at the `set_run_tenant` seam where the repo first exists. Untenanted inline runs get `None`.\n\n`run_number` on `RunSummaryDto` / `RunStatusResponse`; both list SELECTs + the row mapper + a `get_run` getter; `openapi.json` and the UI `schema.ts` regenerated.\n\nShared `RunNumber` gutter renders `#N` (sequential, scannable down the list) with a short-internal-id fallback; the run headline is promoted to the primary line with origin facts beneath; the run-detail breadcrumb reads `repo › #N` and the bar carries the opaque UUIDv7 as a distinct copyable **RUN ID**.\n\nPer-repo monotonic allocation + summary exposure, **in-memory and against real Postgres** (migration `0034` + the `ON CONFLICT` SQL exercised). `cargo check` clean; 171 server + 48 db-postgres tests pass; UI typechecks.\n\nThe run-number gutter supersedes the ADR §4 trigger-cell placement.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}