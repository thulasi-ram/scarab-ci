{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774750,"nonce":"Wajl3J1UKXlv+FW87iNY6Yh1t9M=","title":"fix(drain): in-Pod drain resource story — bounded memory, non-regular-file refusal, token-ceiling clamp (16a7768)","message":"\u003e Imported from pull request #106 (MERGED)\n\u003e Branch: `fix/16a7768-drain-resources` → `main`\n\u003e Created: 2026-08-26T15:13:21Z · Merged: 2026-08-26T15:23:56Z\n\nCloses git-bug 16a7768 (items 1–3; item 4 died with the Postgres ledger).\n\n- **Memory**: scan stream-hashes (no more whole-file read to hash); a transfer byte budget (default 256 MiB, semaphore over bytes in flight, over-budget blob runs alone) bounds BOTH the drain uploads and the fetch downloads; optional k8s requests==limits on both wsfetch containers via `SCARAB_WSFETCH_MEMORY_MIB`/`_CPU_MILLIS` (helm `workspace.helperMemoryMib`/`helperCpuMillis`) — **opt-in** per review ruling, unconfigured deployments keep the pre-ticket Pod shape byte for byte; a set memory limit derives the client budget (half the limit) on one knob.\n- **Non-regular files**: a FIFO/device/socket in /workspace now fails the scan naming the path and kind instead of hanging `fs::read` forever; opens are `O_NOFOLLOW|O_NONBLOCK` + fstat-checked.\n- **Token ceiling**: step `timeout:` capped at 23h at pipeline validation and at the server default knob, so a drain can never outlive its workspace token (exp = timeout + 600s grace, cap 24h); compile-time assert ties the constants.\n- Also fixes `tests/warm_eviction.rs`, which was compile-broken on main since the ADR-0065 cache API change.\n\nReview: skeptical reviewer verified semaphore math, sha256 chunk-boundary equivalence, the 23h assert, and ruled the limits default opt-in. Suite (with the e140121 stack on top): 973/973.\n\n**Merge order: this PR first** — #e140121 stacks on it, and the other three group-1 PRs need its warm_eviction fix on main for a green gate.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}