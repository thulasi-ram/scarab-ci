{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":3,"timestamp":1785098698,"nonce":"GD9zGUsolUNStp15nzjEVEl5/Q0=","message":"The stepping stone this ticket deletes is now REAL and in the standard path (ADR-0061 s3-feed, commit 3d0d289). The acceptance criterion is unchanged and now has concrete targets — closing this means removing ALL of:\n\n- `docker/wsfetch/` and the `scarab-wsfetch` bin target in `crates/scarab-workspace-client/Cargo.toml`;\n- `scarab_executor_k8s::WorkspaceFetch`, `DEFAULT_WSFETCH_IMAGE`, `workspace_fetch_container`, `workspace_feed_is_satisfiable`, `eager_fetch_total`, and the `fetch` parameter of `build_pod`;\n- the `scarab-wsfetch` entries in `.github/workflows/image.yml` (two matrices);\n- `workspace.fetcherImage` in `deploy/helm/scarab/values.yaml` + `SCARAB_WSFETCH_IMAGE` in the ConfigMap and in `config.rs`'s knob table;\n- the build + `kind load` block in `deploy/local-proc/up.sh` and the `docker build` in `just local-helm local`.\n\nEvery one of those sites carries a `DELETE ME with the driver (git-bug 0628369)` comment, and the ONE line s2b actually replaces is marked: the `empty_dir` expression for `WORKSPACE_VOLUME` in `build_pod`.\n\nThe three anti-calcification guards from D2.3 are live: (1) `scarab-wsfetch` prints `mode=eager (ADR-0061 s3-feed stepping stone — the node driver replaces this)` in every Step Pod's own log AND the control plane logs it at WARN + bumps `eager_fetch_total()`; (2) the marked volume-kind line; (3) `deploy/local-proc/up.sh`'s Pod-reachability discovery is now a HARD gate rather than a warning. Guard (2) of the ADR's own list — `nodeDriver.enabled=true` and `just local-helm` failing if the DaemonSet is not Ready — is s2b's to add, since the DaemonSet does not exist yet.","files":null}]}