{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774747,"nonce":"EXuagEXETqVVIuOGTtKXrH9q1Sw=","title":"Delete the cancelled Export/Farm surface (~12.5k lines, 0ec3b39)","message":"\u003e Imported from pull request #99 (MERGED)\n\u003e Branch: `chore/0ec3b39-delete-export-farm` → `main`\n\u003e Created: 2026-08-25T07:22:01Z · Merged: 2026-08-25T08:06:24Z\n\nExecutes git-bug `0ec3b39`: delete the cancelled Export/Farm lazy-delivery surface (ADR-0066 decision, deletion order in point 10). **Net ~−12,500 lines. 886/886 tests green** (real PG + MinIO); reviewed by a skeptical deletion reviewer (both failure modes hunted: nothing live died, nothing dead survived — two prose/coverage warts found and fixed in the final commit).\n\n**Five commits:**\n- `7bd1d33` s1 — the Export lifecycle cut out of `workspaced.rs` (−2,868): routes, handlers, `WorkspaceState.farm/.exports/.captures`, `WsError::Export`; `sweep_exports_once` → `sweep_residue_once` keeping the fence-residue row sweep and the abandoned-pack-session abort byte-identical; ~15 Export tests deleted, `ExportHarness` → `DepotHarness`, all 20 fence/drain/scatter tests survive with assertions unchanged (reviewer-verified).\n- `953100a` s2 — `export.rs`, `farm.rs`, **`settle.rs` wholesale** (−8,139). Deviation from ADR-0066's \"Export half of settle.rs\", justified by the verified call graph: every settle entry point terminates in the deleted `settle_export`; the live drain path never touches it. Noted in the ADR tick.\n- `3742246` s3 — **statcache, deliberately its own droppable commit** (−1,696): orphaned once its last consumers died; revert just this commit if you want it back.\n- `524f334` s4 — prose collateral + the dated ADR-0066 point-10 tick with both deviation notes.\n- `6737af7` — review touch-up: `restore_dir_metadata` docs name the surviving caller (`Cas::materialize`), the deepest-last ordering test re-homed at the scarab-storage-s3 grain (`directory_metadata_is_applied_children_before_parent`), changeset mod-doc tombstone.\n\n**Carve-outs honored (ADR-0066 point 10):** `changeset.rs` + overlay test tier untouched (the revival package); `reflink-copy` stays as the blessed-primitive workspace-manifest note; Justfile substrate recipes kept. Untouched by construction: the entire ADR-0067 drain path, pack sessions, tombstones, linger ticker, fence-residue sweep.\n\nCloses `0ec3b39` and finding `1a9df08` (the warm-bytes gauge is truthful again — its help text now matches what the tier holds).\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}