{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1784306814,"nonce":"lWVu+XhjR1BKVGKVnTWLVBi9AN8=","title":"[dogfood] in-cluster runs dead-letter: step pod exits 0 but never settles to Succeeded","message":"Running scarab-server IN-CLUSTER (Helm, ServiceAccount RBAC) on colima, every run dead-letters. The clone step's Pod runs and its 'step' container terminates exitCode 0 (clone 'ready at /workspace'), but the scheduler never transitions the step Pending-\u003e...-\u003eSucceeded. Its launch_step outbox message is re-claimed until delivery_attempts hits 10, then dead-letters -\u003e run dead_lettered, all steps cancelled. The downstream step never launches.\n\nReproduced twice (runs fe739ace, f47845ce), both identical. The SAME code as a HOST-BINARY server (full kubeconfig, namespace default) settled fine and went green (run 68af6899) — so this is specific to the in-cluster execution (ServiceAccount in namespace 'scarab').\n\nRBAC is NOT the cause: the chart Role grants pods (get/list/watch/create/delete/patch), pods/log(get), pods/status(get), pods/exec(create,get). \n\nThe settle failure is COMPLETELY SILENT — no error/warn, and no poll/settle/exec/snapshot line even with RUST_LOG=info,scarab=debug. The only noise is the known log-tail PodInitializing spam (c653742). So step 1 is instrumentation: the scheduler poll-\u003esettle path (scheduler.rs ~850: executor.output/results/artifacts then finalize_step) must log why it doesn't reach Succeeded — a swallowed Err there would explain the silent redeliver-to-dead-letter. Suspect the k8s executor's post-step settle (workspace snapshot exec / results-egress sidecar release / artifact harvest) erroring under the in-cluster SA/namespace and being dropped.\n\nBlocks a green in-cluster (Helm) dogfood run. The host-binary path is the current known-good.","files":null}]}