{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787588009,"nonce":"xHc06t+HCIcb2PImMWNi9WNM7+A=","title":"ADR-0067 part 12 shipped layer 1: every wire surface accepts algorithm-tagged addresses","message":"(sha256:\u003chex\u003e), pack footers and depot_pack_members rows are born tagged, and readers\naccept both tagged and legacy bare spellings. The hash swap itself was deliberately left\nout of that branch (PR #95).\n\nThe follow-up: introduce blake3: as a second accepted tag and make it the default for\nNEW content. Constraints established when this was decided (2026-08-24):\n- Nothing is rewritten; historical sha256: roots keep their identity forever. Mixed\n  trees are legal — a new blake3 tree may reference unchanged sha256 subtrees (keeps\n  dedup against existing content; the tag is what makes mixed unambiguous).\n- BLAKE3 is a registered REAPI digest function, so the swap stays inside the standard's\n  vocabulary.\n- Verified facts (2026-08-24, primary sources): the blake3 crate (1.8.x) does NOT ship\n  range/slice verification itself — that is bao (0.13.x, \"beta cryptography\", ~6.25%\n  outboard overhead at fixed 1 KiB chunks; chunk-group variants live in forks abao /\n  n0's bao-tree). So do NOT justify the swap on verified ranged pack reads until that\n  ecosystem is chosen deliberately; justify it on hash throughput (BLAKE3 is ~14x\n  SHA-256 and parallelizes) on the scan/drain leg the measurements showed is\n  hash-dominated.\n- Entry points when built: scarab-storage hash_hex/HashAlgo (Sha256 is currently the\n  only variant), parse_address/tagged_address, wsfetch scan_dir hashing, and the\n  /have + closure-validation compare paths (compare within a tag namespace, never\n  cross-hash).\nReferences: ADR-0067 part 12; ADR-0066 point 11; the tag work landed in PR #95.","files":null}]}