{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774748,"nonce":"Oc1JKN9rxWBSFrTxQl4Hl28A88Y=","title":"The borrow ledger: fence-grain dedup debts, recorded at the record transaction (ec294b7)","message":"\u003e Imported from pull request #101 (MERGED)\n\u003e Branch: `feat/ec294b7-borrow-edges` → `main`\n\u003e Created: 2026-08-25T19:26:53Z · Merged: 2026-08-25T21:41:46Z\n\nThe borrow ledger (git-bug `ec294b7`) — stacked on **#100** (the reclaimer; this branch builds on its in-transaction re-check). **896/896 green.** Full chain again: design (4 candidates) → skeptical audit → independent judge → build → review — and the chain caught two would-be time bombs before a line shipped:\n\n- **The audit:** \"borrower lifetime = record lifetime\" was wrong by orders of magnitude — drain records are residue-swept at ~24h while the control plane keeps snapshot roots live for months (pins: indefinitely). Deletion gated on records-as-they-were would have freed packs under live readers.\n- **The judge:** the audit's own fix (exempt success records from the sweep) would have deadlocked the epoch floor — pre-epoch success records now living forever would block all committed expiry, permanently. The shipped predicate carves them out.\n\n**What ships:**\n- `depot_fence_borrows` (migration 0048): fence-grain edges — when fence B's success record dedups against fence A's committed packs, one row per (B, A) records the debt, **inside the record transaction**, keyed on the same tagged closure the durable-presence re-check certifies. One `INSERT..SELECT` per drain; zero cost on PUT and `/have`.\n- The residue sweep now keeps post-epoch success records alive until fence expiry deletes them (error and pre-epoch records stay TTL-swept) — the borrow anchor lives exactly as long as the fence.\n- `FOR SHARE` on the re-check (DISTINCT dropped — Postgres refuses the combination): record-vs-expiry races resolve to a 422 re-drive or an expiry skip, both constructed in tests at the SQL grain, both orders.\n- Applied migrations 0043/0044 stay **byte-frozen** (an earlier revision edited their comments — sqlx checksums would have `VersionMismatch`ed every existing DB; prose relocated to 0048's header and the code).\n- Successor ticket `6499fb1` carries the full deletion contract for the actual expiry pass: one victim fence per transaction, borrower-liveness gate, epoch floor, bytes through the shipped rowless reclaimer.\n\nCommitted-pack deletion is still forbidden — but from this merge on, every drain writes down who leans on whom, so the day expiry ships, the ledger it needs already has history.\n\nCloses `ec294b7` on merge.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}