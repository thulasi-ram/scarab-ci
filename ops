{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1784231813,"nonce":"H46XnryRQDYPgItRXYqKJnmSiyQ=","title":"Wire kind: build to actually dispatch as a rootless BuildKit Pod, and add registry auth. Today Executor::launch always calls build_pod; build_pod_for_build / BuildSpec / push_fence exist but are referenced only by tests (executor-k8s:462-533). Route build steps to build_pod_for_build. Registry auth = a generic scoped secret (ADR-0018 amendment / ADR-0037): a dockerconfigjson-shaped secret injected into BuildKit for push + private-FROM pull; plus optional forge-derived credential for pushing to the forge's own registry (GHCR via installation token) from the ForgeConnection.","message":"- [ ] kind: build steps dispatch as build_pod_for_build (rootless BuildKit), not the plain build_pod\n- [ ] Registry auth via a scoped dockerconfigjson secret; push + private base-image pull work\n- [ ] Forge-derived push credential works for the forge's own registry (zero-config)\n- [ ] Push fenced via push:{image}@{digest}; ImageArtifact recorded with digest\n- [ ] Live Colima test: build a trivial image and push to a local registry\n\nNone - can start immediately (registry-derived cred path benefits from ADR-0046 d54e9be)","files":null}]}