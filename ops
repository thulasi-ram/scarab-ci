{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787548165,"nonce":"gqTnYgdRVRCkxpHjkKK9D1AswM0=","title":"The 0067 branch made reads and records replica-independent (PG-backed ledger/records, /have off the durable index, ranged pack reads), but the drain WRITE path is still replica-local: in-flight pack sessions live in one replica's memory (workspaced.rs PackSession map) and validate_drain_closure reads the LOCAL warm dir for trees. A drain's 16 parallel PUTs through the plain ClusterIP scatter across replicas, so bytes stage in sessions that never seal and the POST-receiving replica 422s — permanently, since retries re-scatter.","message":"replicaCount \u003e 1 therefore remains unsafe for drains. The chart (service-workspace.yaml comment, values.yaml replicaCount note) and the ADR-0067 implementation notes say so explicitly since 2026-08-24.\n\nFix directions to evaluate (owner input wanted on topology):\n- fence-scoped routing: the fetcher resolves one replica per fence (headless Service + deterministic pick, or a redirect from any replica), so a drain's PUTs and POST share a replica by construction;\n- OR shared validation: closure validation reads warm-OR-pack-index for trees too, and pack sessions move off replica memory (e.g. each PUT streams its own single-part pack object; the drain seals by listing).\nJudge finding A1 (review 2026-08-24); reviewer finding R2's overwrite class is fixed separately by session-unique pack keys.","files":null}]}