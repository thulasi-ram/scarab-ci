{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774752,"nonce":"Fs6BhkTIymdf6JBfBvObWt0dA2M=","title":"feat(depot): blob reads move from authenticated to authorized (52ef3aa)","message":"\u003e Imported from pull request #112 (MERGED)\n\u003e Branch: `feat/52ef3aa-blob-authz` → `main`\n\u003e Created: 2026-08-26T16:29:17Z · Merged: 2026-08-26T18:49:03Z\n\nCloses git-bug 52ef3aa (ADR-0061 follow-up; the gap had grown — ranged GET and HEAD post-date the ticket).\n\nAny valid workspace token could read any blob; only trees were checked against the roots claim (justified by 256-bit-unguessable addresses — a secrecy claim about addresses this system itself reprints). Now blob GET + ranged GET + HEAD are authorized against the token's roots closure:\n\n- **Mechanism**: per-token (SHA-256-of-token-keyed) in-memory allowlist; populated free as a side effect of the token's authorized `/flat` (the hot path's own call — steady state pays one hash + one lock + a binary search, zero walks); singleflight fallback closure walk on miss; 128 MiB LRU with a 200k-blob per-entry threshold (pathological tokens walk uncached rather than thrash the table). No wire change; old tokens keep working.\n- **The security keystone** (audit F1, independently re-verified by the reviewer): the fallback walk's roots come EXCLUSIVELY from the token's roots claim — the write ledger is never an input to blob authorization, killing the ledgered-tree→foreign-blob escalation. Pinned by a named must-pass test.\n- **Fail directions**: 403 only from a COMPLETE walk proving non-membership (body names the blob + the caller's own fence); an incomplete walk is a 500, never a 403. Client-side, a 403 rides the existing Denied→exit 3 path (non-retryable, bounded) — tested to not consume the retry window.\n- **Rollout**: `SCARAB_DEPOT_BLOB_AUTHZ=off|log|enforce`, default **log**; `scarab_depot_blob_authz_would_deny_total{mode}` + walks counter + sampled warn naming fence+blob; flip to enforce on a zero counter over a representative window. Helm `workspace.blobAuthz`. Log-mode caveat documented: an unwalkable root 500s allowlist-missing reads (intended fail-closed-on-availability).\n- Browse (UI) tokens bypass by design, double-excluded from population. `/have` stays an accepted, now honestly-booked 10k-bits-per-request existence oracle.\n\nChain: re-verify (ticket predated ADR-0067) → design → adversarial audit (7 findings; the keystone invariant, 403/500 fail-direction split, per-entry cap) → judge → build → adversarial review (MERGE-READY, zero fix-first; keystone re-verified independently). Suite: **988/988**.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}