{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774732,"nonce":"libWIzyNTUCGyz5GNCWJwFC1PA8=","title":"test(executor-k8s): kind-in-CI tier — live cluster job + new cluster cases (test-strategy Phase 2)","message":"\u003e Imported from pull request #65 (MERGED)\n\u003e Branch: `test/phase2-kind` → `main`\n\u003e Created: 2026-07-24T04:27:27Z · Merged: 2026-07-24T05:48:38Z\n\nImplements the kind-in-CI tier of Phase 2 of the test strategy (docs/audits/2026-07-24-test-strategy.md, planned in #60).\n\n\n- Triggers on PRs + pushes to main, **path-filtered** to the execution path (`crates/scarab-executor-k8s/**`, `crates/scarab-engine/**`, `crates/scarab-server/**`, `crates/scarab-storage*/**`, `docker/**`, and the workflow itself).\n- Creates a kind cluster (helm/kind-action), loads the clone + results-sidecar step images from the GHCR `edge` tags — or builds them from the tree when the change touches `docker/**` (dorny/paths-filter) — deploys a throwaway in-cluster registry, resolves the Pod-reachable host IP for the sidecar-egress stub, exports the `SCARAB_TEST_*` gates, and runs the full ignored cluster suite via `cargo nextest run --run-ignored all --no-fail-fast`.\n- **Advisory probation**: this job is intentionally NOT a required check. Live-k8s runs carry inherent flake surface; it reports, humans read it. Promote to required only after a quiet track record. (Stated in the workflow header comment too.)\n- Since the workflow path-filters on itself, the kind job runs on this very PR.\n\n\n- `run_as_root_is_reflected_on_the_admitted_pod` — the ADR-0039 grant (uid 0, `runAsNonRoot: false`, still unprivileged/non-escalating) and the hardened default baseline, read back from the Pod the API server **admitted**, not from the locally built spec.\n- `image_pull_failure_fails_the_attempt_fast` — a nonexistent image surfaces a terminal `Failed { class: Infra { never_started: true } }` fast (ADR-0047) instead of hanging the attempt `Pending` forever.\n- `rerun_supersede_tears_down_the_in_flight_descendant_pod` — the **live** orphan-Pod teardown proof (ADR-0056 amendment, git-bug `fd6e6d4`): drives `rerun_step` + `Scheduler::reconcile_supersessions` over the in-memory Db with the REAL executor and asserts the superseded sleeper's Pod is actually gone from the namespace. **Passes against current code** — the engine-side fix is in; this pins it against regression.\n- Cancel-path teardown already had a live case (`cancel_tears_down_a_running_pod`), so no duplicate was added.\n\n\nAll three new cases ran green against a live cluster (colima k3s, `default` ns):\n\n```\nPASS run_as_root_is_reflected_on_the_admitted_pod        (0.1s)\nPASS image_pull_failure_fails_the_attempt_fast           (2.1s)\nPASS rerun_supersede_tears_down_the_in_flight_descendant_pod (37.4s)\n```\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}