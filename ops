{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774710,"nonce":"Rm+0tv13twC0bgAIAsOzWYJP6Tc=","title":"feat(web-ui): light-first \"engineering blueprint\" re-skin (Convey-influenced)","message":"\u003e Imported from pull request #8 (MERGED)\n\u003e Branch: `feat/web-ui-convey-reskin` → `main`\n\u003e Created: 2026-07-14T20:16:41Z · Merged: 2026-07-14T20:16:52Z\n\nRe-skins the SolidJS control plane around a cool paper-white worksheet with a dark pine-black console, keeping the pine/black identity. Emerald is the one interactive accent; gold shows up only as an edge — corner rivets, hairlines, doodle ink, and the rare token (SHAs, trigger glyphs). `docs/DESIGN.md` §0–§9 rewritten to match.\n\n- **Light default** (`:root`) + opt-in **dark carapace** (`[data-theme=\"dark\"]`). Semantic tokens are re-pointed per theme so every rule re-themes. Toggle in `theme.ts`, persisted in `localStorage`, with a pre-paint guard in `index.html` (no flash).\n- **Logs + DAG stay pine-black \"terminal\" panels in both themes** — the machine surface always looks like the machine.\n\n- **Corner registration rivets** (a single `::after` overlay, so they sit cleanly on any bordered/filled corner) on repo cards, the run summary block, and the two CTA buttons — used sparingly.\n- **Space Grotesk** for display/titles, **Inter** for body/UI, **JetBrains Mono** for all chrome/labels/machine tokens. Faint dot-grid page texture.\n\n- Collapse **Branches / Pull-requests** tabs into **branch + trigger filters** on the runs list (tabs: Runs · Environments · Secrets · Settings).\n- **Match DAG and Logs panel heights** on the run screen.\n- Remove top-bar breadcrumbs; the big page header carries context. On a run, the repo becomes a **header-sized breadcrumb link**.\n- **Contextual header CTA** per tab (Run pipeline / New environment / New secret), dropped to its own row below the title on both repo and run screens.\n- Enlarge the navbar wordmark.\n\nDriven live across all routes (repos, repo shell, runs table, environments, secrets, run detail with DAG + logs, run/env dialogs) in **both light and dark**. `tsc --noEmit` and `vite build` pass; zero console errors.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}