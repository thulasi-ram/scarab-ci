{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787703299,"nonce":"6td26KvdW1wJgAGn59VIwMxQ+74=","title":"Found by the skeptical review of PR-pending feat/6499fb1-committed-expiry (finding 6, MEDIUM-LOW,","message":"liveness not safety). The committed-expiry pass's recorded arm nominates candidates with\nORDER BY posted_at LIMIT 100 under the LOOSEST cutoff across all retention profiles; per-run\nprofile cutoffs are then enforced in code (continue) and again inside the victim txn.\n\nStarvation shape: \u003e= 100 old runs on a long-TTL profile (e.g. keep-forever-ish 36500d) permanently\noccupy the nomination window; every pass re-fetches and re-skips exactly them, and newer,\ngenuinely-expired victims behind the window never get nominated until the blockers age out.\nExpiry silently stops — in the retention-SAFE direction (nothing wrongly deleted), but with no\nmetric distinguishing \"nothing eligible\" from \"window starved\".\n\nFix directions (either): push the per-run cutoff into SQL (bounded CASE over the registry's\nprofiles, resolved at pass start — registry is small), or keyset-paginate past in-code skips\nwithin a pass (carry the last posted_at, iterate until a batch yields work or the table ends).\nEither way: add a starved/skipped-candidates counter so the condition is visible.\n\nAlso noted same review (perf, LOW): the reachability floor's EXISTS scans runs.created_at without\nan index — a full pre-epoch scan every 300s; add the index when touching this.\ndepot_expiry.rs is the file; tests follow the existing DepotHarness patterns.","files":null}]}