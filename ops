{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":3,"timestamp":1785127881,"nonce":"NdmPqGjUGoGACLtVcG8GscAPclQ=","message":"Partially done on PR #93. The IRREVERSIBLE subset landed; the pre-existing debt is what this ticket now tracks.\n\n## Landed (commit f6363a9 and neighbours)\n\nScoped deliberately to names coined *on this branch*, i.e. cheap now and a data\nmigration or compat shim after merge:\n\n- migration `0039_run_workspace_pin.sql` -\u003e `0039_run_snapshots_pin.sql`, columns\n  `runs.workspace_pinned_at/_by` -\u003e `snapshots_pinned_at/_by`, index likewise\n  (verified `0039` is NOT on `main`, so editing in place was correct)\n- persisted event kinds `RunWorkspacePinned`/`RunWorkspaceUnpinned` -\u003e\n  `RunSnapshotsPinned`/`RunSnapshotsUnpinned`, plus the UI mirrors in\n  `ui/scarab-web-ui/src/events.ts` (highest cost-of-delay item: these are\n  persisted strings)\n- route `/v1/runs/{id}/workspace-pin` -\u003e `/snapshot-pin`, with operationIds\n- `ANNOTATION_WS_IDENTITY`: `scarab.io/workspace-identity` -\u003e\n  `scarab.io/snapshot-identity` (it was coined in the very slice whose point is\n  that identity != root)\n- the DTO/port surface coined this session: `WorkspaceRetention*` -\u003e\n  `SnapshotRetention*`, `pin_run_workspace` -\u003e `pin_run_snapshots` and siblings\n  across port -\u003e adapter -\u003e testkit -\u003e server, `SCARAB_WORKSPACE_INPUTS` -\u003e\n  snapshot-roots naming, and `ui/.../workspace-retention.ts` -\u003e\n  `snapshot-retention.ts` with its user-facing copy\n\n## Still open — pre-existing debt, the actual scope of this ticket\n\n`StepSpec.workspace_inputs`/`workspace_outputs` and everything downstream ·\n`gc_workspace_roots`/`forget_workspace_root` · `workspace_cas`/`with_workspace_cas` ·\n`workspace_ttl_ms` · `retention_workspace_days` + `SCARAB_RETENTION_WORKSPACE_DAYS` ·\nthe whole browse surface (`GET /v1/runs/{id}/steps/{step}/workspace[/file]`,\n`WorkspaceListing`, `WorkspaceEntryDto`, `WorkspacePathQuery`, `workspace_walk`,\n`workspace_segments`, `list_workspace`, `get_workspace_file`, `listWorkspace`,\n`workspaceFileUrl`, and `StepPane.tsx`'s \"workspace\" tab -- whose own comment\nadmits it shows the scoped attempt's immutable snapshot) · `ANNOTATION_WS_ROOT` /\n`ANNOTATION_WS_INPUTS` · `drive_workspace`.\n\n## Correct as-is -- do NOT rename\n\nThe *service* is legitimately \"the workspace service\": `Role::Workspace`,\n`WorkspaceServiceConfig`, `mod workspaced`, `WorkspaceState`, `workspace_token.rs`,\n`WorkspaceClient`, the `scarab-workspace-client` crate name, the\n`SCARAB_WORKSPACE_*` service knobs, the Helm `workspace.*` values, and the\n`scarab_workspace_*` metric prefix (widest blast radius, lowest value).\nThe *mutable Workspace* is legitimately named at `WORKSPACE_MOUNT_PATH`,\n`WORKSPACE_GID`, `WORKSPACE_VOLUME`, `is_workspace_pod`, and every `/workspace` path.\n\nHazard for anyone doing the rest: the **Cargo** homonym. Exclude root\n`Cargo.toml`'s `[workspace]`, `.workspaces/` worktrees, and `mock.ts`'s\n`cargo test --workspace` string. Do not run a blind sed.","files":null}]}