{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":3,"timestamp":1785565393,"nonce":"4FggRPykhiMbexygY5ICNjv75GU=","message":"**Completed at `63e3c0b` + `e30f594`.** The architect ruled a Depot outage may fail Attempts (the Depot is to be treated as a real dependency — distributed/masterless later), which unblocked the control-plane half.\n\nThe remaining two boxes:\n- [x] `TieredCas::ingest`'s two walks — DELETED; the trait route is now a loud refusal so an unseen `dyn Cas` caller fails at once. `drive_workspace` = warm ingest (client walk, `/have` dedup) → prune/identity via a CP `DrainCas` (writes warm, reads tiered) → ONE awaited `POST /v1/cas/flush {root: published}` → only then the root annotation + sidecar release. The Depot PUT handlers went WARM-ONLY in the same commit — without that the flush RPC would have degenerated to heads and no latency was won (red-team finding).\n- [x] A flush failure names the cause on the Attempt — via 4cf03d7: `DriveErr::EvidenceLost` → `ExecState::Failed{Infra{never_started:false}, cause}` → `attempts.failure_detail` (migration 0041) + `AttemptFinished.cause`.\n\nThe prune barrier from comment #1 is answered by inventory-from-the-published-root: the Depot walks the closure from the post-prune root, so prune-minted trees, reused baselines, and inherited sub-trees are in the batch by construction.\n\nSuite: 938 passed / 0 failed / 26 env-gated skips. The kind tier now executes the exact warm-ingest→flush→patch path and the part-4 durability test walks the whole published closure through a COLD-ONLY handle at the instant of green (it previously read the tiered handle — warm-cache presence, proving nothing; skeptical-review catch).","files":null}]}