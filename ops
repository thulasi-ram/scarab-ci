{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":3,"timestamp":1785565419,"nonce":"YwrzEDfcZmj9jtmbf2hZQSujWnE=","message":"**Done at `63e3c0b` for the drain path.** Classification: a Fatal flush answer (only `FlushError::Mismatch` maps fatal — warm-NotFound is retryable because the next poll re-drives and re-uploads via `/have`, red-team catch) becomes `DriveErr::EvidenceLost{cause}` immediately; transient drain failures escalate after 5 MINUTES of consecutive failure (Pod annotation `scarab.dev/ws-drain-first-failure-ms` as the clock — a count-based budget at 500ms poll cadence was 2.5s, i.e. every Depot rollout would have EvidenceLost'd all in-flight drains). Only drain legs touch the clock; artifact-harvest failures stay plain Transient. `poll` maps EvidenceLost through the NORMAL Failed path (the OutputContract precedent) with `cause`, so it reaches settlement instead of looping to a timeout: `ExecState::Failed.cause` → `attempts.failure_detail` (migration 0041, expand-only) → `AttemptFinished.cause` (`serde(default)`, old rows verified). Retry stays author-gated (`retry:` budget) — the at-least-once bound is unchanged. OutputContract's message, which previously died in eprintln, now rides the same field.\n\n- [x] fails without waiting out the step budget (bounded at 5 min)\n- [x] the Attempt carries the cause (failure_detail + event)\n- [x] transient vs permanent distinguished; permanent does not present as timeout\n\nResidual, filed separately: 66c93be — if the annotation write itself fails every poll (RBAC), the clock never anchors and the old timeout disguise returns.","files":null}]}