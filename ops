{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1785697198,"nonce":"lOBvhHG0ah7bLbzzIjUhp374jE4=","title":"Established 2026-08-03. The architect wants the Depot highly available, not a singleton. Yesterday's answer was shard-by-fence with a drain protocol; **cancelling the lazy Export feed (git-bug 4ce7f2c) makes it far simpler.**","message":"**State classification** (all under one RWO PVC; `WorkspaceState` is the whole surface, `workspaced.rs:280-318`):\n- **Immutable content-addressed** — `blobs/`, `trees/`, `farms/`. Replicate trivially; N replicas cost only duplication, since `/have` answers per-replica.\n- **Per-fence mutable** — write ledgers (`ledgers/\u003cfence_key\u003e`, \"the disk is the only copy, deliberately\", `workspaced.rs:1154-1204`) and drain records (`drains/\u003cfence_key\u003e/record.json`, `:2299-2438`). The ONLY blockers. They fail **closed and loudly** (a split ledger makes `validate_drain_closure` refuse a drain whose bytes all landed; a drain GET to the wrong replica 404s -\u003e FatalConfig), and they are **short-lived** — swept at `FENCE_RESIDUE_TTL_SECS` = the 24h workspace-token max TTL plus grace.\n- **Coordination** — ALL Export-only (`FarmLease`, the first-client pin, `SettleInputs`' mutex, the captures map) and therefore **deleted with the Export machinery**. The sweep leader lease is Postgres-backed and lives in the CONTROL PLANE (`retention.rs:27-49`), already multi-replica-safe; the Depot has none and structurally cannot (it never connects to Postgres).\n\n**Smallest design that works:**\n- **HA reads need NOTHING.** Content is immutable; the control plane's `TieredCas` already has `fall_through_on_warm_error` (`main.rs:247`). Any replica serves any blob it holds; a miss is a miss.\n- **Safe writes need FENCE AFFINITY, not sharding.** Route by `fence_key` (the hash already exists and both clients already compute it) so a fence's PUTs, its drain POST, the control plane's drain GET and its flush all reach one replica. Client-side or Service-level routing; no data migration, no Postgres in the Depot.\n- Rejected alternative: moving ledgers/drain records to Postgres — cleaner, but breaks the Depot's no-database invariant.\n\n**Blocked on / notes:**\n- Gated behind Export/Farm removal (~10k lines across `export.rs`/`farm.rs`/`changeset.rs`/`settle.rs`), with the fence-residue sweep carefully EXTRACTED from `sweep_exports_once` first.\n- The flush's `503 retryable` reasoning explicitly assumes ONE replica (`workspaced.rs:2017-2023`) and must be re-argued under affinity.\n- Today: `replicaCount: 1` behind a plain ClusterIP with **no `sessionAffinity`**, and `values.yaml:237-242` calls \u003e1 \"unproven\".\n\n- [ ] Export/Farm machinery removed, fence-residue sweep preserved\n- [ ] Fence-affinity routing for writes; reads unrouted\n- [ ] The flush's retryable-vs-fatal reasoning re-argued under N replicas\n- [ ] `replicaCount: 3` actually exercised in `just local-helm` — \u003e1 has never been run","files":null}]}