{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":3,"timestamp":1784246323,"nonce":"ndZRQeXXe0NKXXEOFIxRzlOq2oE=","message":"Implemented on feat/arc-abc-impl. LIVE-VERIFIED in token mode against real GitHub (repo thulasi-ram/scarab-ci via gh auth token): the full ForgePort contract suite passed over real HTTP — latest_commit, read_file_at_ref (raw media type), list_dir_at_ref (Link-header pagination), normalize, set_status (real status posted with run deep-link), register_webhook (no-op by design), get_permissions, mint_checkout_credential.\n\nREMAINING FOR A HUMAN (needs a real GitHub App, which I cannot create):\n1. App mode end-to-end: create a GitHub App (permissions: contents:read, statuses:write, deployments:write, issues:write, members:read for permissions endpoint), install it on a test repo, then run:\n   SCARAB_TEST_GITHUB=1 SCARAB_TEST_GITHUB_APP_ID=\u003cid\u003e SCARAB_TEST_GITHUB_PEM_FILE=\u003cpem\u003e SCARAB_TEST_GITHUB_REPO=\u003cowner/name\u003e SCARAB_TEST_GITHUB_SHA=\u003cmain sha\u003e cargo test -p scarab-forge-github --test contract_live -- --ignored\n   This exercises the JWT -\u003e installation-token exchange + cache and the DOWNSCOPED contents:read single-repo token minting (the ADR-0045 clone credential) that token mode cannot.\n2. GHES: point with_base_url at a GHES host if/when available (code path identical, only the base URL differs).\n3. Secondary-rate-limit backoff fires on 403/429+Retry-After (bounded, capped 60s) — only observable under real rate pressure.","files":null}]}