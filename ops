{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774725,"nonce":"MDqJnnsCIKhW/RZaxwVXhhW/Vuw=","title":"docs: refresh docs site, READMEs, and app screenshots","message":"\u003e Imported from pull request #48 (MERGED)\n\u003e Branch: `docs/site-refresh-and-screenshots` → `main`\n\u003e Created: 2026-07-21T04:09:18Z · Merged: 2026-07-21T07:49:27Z\n\nBrings the docs site and READMEs back in line with the current product and regenerates the app screenshots (dark theme). Docs-only refresh — no code/behaviour changes.\n\n- **index** — drop stale \"GitHub adapter unimplemented\" framing (adapter is implemented; Forgejo adapter also exists); move live-k8s exec, results sidecar, and push-CI out of \"in progress\"; keep an honest \"not hardened at scale\" note.\n- **get-started/run-locally** — fix `dev/` → `deploy/local-proc/` paths; add `just docs` / `just local-helm`.\n- **get-started/deploy-helm** — rewrite to the real chart story: the chart deploys Scarab on **any** Kubernetes (local kind/colima *or* a live cluster), with `just local-helm` as a convenience loop on top; adds a `values.yaml` example.\n- **configure/reference** — fix env-file path; expand env-var table from `config.rs`; fix broken ADR links.\n- **guides/authoring** — expand the stub into a full guide (triggers, launch inputs, clone, invoke/matrix, named results, environments/secrets/gates, placement, reruns); every YAML snippet verified against real fixtures/serde structs.\n- **README + helm README/values** — correct GitHub-adapter framing + crate names, `main.rs` → `config.rs` config reference, document the Forgejo webhook secret.\n- Clear stale WIP/stub sidebar badges + the reference-page WIP note.\n- ADR references pulled out of inline prose into a `## References` citations section at the bottom of each page (docs pages + both READMEs).\n\n- Replace **dashboard** and **run-detail**; add **environments** and **secrets** figures to the authoring guide.\n- README standardized on single dark images; stale `-light` variants removed.\n- Screenshots produced via a temporary frontend mock harness that was **fully reverted** (`ui/scarab-web-ui` is clean).\n\n- Docs site build passes (66 pages, images optimized); all cited ADR slugs resolve.\n- A red-team pass verified the screenshots depict only real UI — it caught one fabricated provenance field on run-detail, which was re-captured and fixed.\n\n- Filed git-bug `f3da2aa` (enhancement): Helm chart lacks a first-class `secrets.forgejoWebhookSecret` value mirroring the GitHub one.\n- Progress/handoff notes: `docs/handoffs/docs-refresh-2026-07-21.md`.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}