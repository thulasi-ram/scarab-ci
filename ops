{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774723,"nonce":"3iWgggFATgM0jbfL5iW1yqgvkLU=","title":"ADR-0057: run trigger-context provenance (headline, PR base, dispatch reason, require_reason)","message":"\u003e Imported from pull request #45 (MERGED)\n\u003e Branch: `feat/adr-0057-run-provenance` → `main`\n\u003e Created: 2026-07-20T20:22:33Z · Merged: 2026-07-20T20:30:37Z\n\nImplements **ADR-0057 — run provenance: trigger context and origin details** (`docs/adr/0057-run-provenance-trigger-context.md`), grilled to *Accepted* and built across four tracer-bullet slices.\n\n\u003e **Stacked on #44** (base = `feat/adr-0056-rerun-retry-language`). Merge #44 first; GitHub will retarget this to `main` automatically. The diff here is ADR-0057-only.\n\n\nThe run **Headline** (`trigger_title`) — one normalized human line per run saying *what it's about* — surfaced on the runs list and run-detail context bar, plus PR base rendering and an environment-gated manual dispatch reason.\n\n| Slice | Commit | Delivers |\n|-------|--------|----------|\n| accept | `d741545` | ADR Proposed→Accepted (7 grill outcomes) + CONTEXT.md \"Headline\" term |\n| **A** | `8d2e1b0` | Push commit-subject headline end-to-end (migration `0032`) + the single shared `TriggerCell` component (runs list + run header) |\n| **B** | `130a7f5` | PR title headline + `base ← head` (migration `0033`, discrete `origin_pr_base`) |\n| **C** | `80dca79` | Optional manual/api dispatch **reason** input (API + New-run form), stamped as the headline |\n| **D** | `313ef6c` | `ProtectionRules.require_reason` enforced at the **admission gate** (HTTP 400, scoped to empty manual/api) |\n\n\n- **One `trigger_title` column**, display-only, subject-only, 200-char char-safe cap, stored clean.\n- **PR base** is a discrete `origin_pr_base` fact (not folded into the headline); rendered `base ← head`.\n- **Manual reason** optional at dispatch; requiredness is a per-Environment `ProtectionRules.require_reason` enforced at admission (not the dispatch endpoint), firing iff `require_reason \u0026\u0026 kind ∈ {manual, api} \u0026\u0026 reason empty`. Push/PR/tag/cron exempt.\n- **One shared Trigger-cell component** across the runs list and run-detail context bar (no more duplicated JSX).\n- **Security boundary (load-bearing):** provenance fields ride `Event` but are **deliberately excluded from `Event::context()`** (the CEL/`${{ }}` map). Shell interpolation of attacker-controlled commit/PR text is the GitHub-Actions script-injection class and has no context-free escape — so the un-encodable sink gets *exclusion*, not escaping; escape stays at known display sinks. Guarded by `*_excluded_from_context_map` tests for all three kinds and a comment at `context()`.\n\n\nEach slice ran `just check` + targeted `cargo test` (incl. real-PG db tests against the dev Postgres) + UI `typecheck`/`build`, all green; the final `just check` at `313ef6c` is the composed-state gate. OpenAPI + UI schema regenerated throughout. **Not yet live-verified** on a running cluster — a fresh dispatch (with a reason) + a PR webhook are needed to see the headline / `base ← head` / `require_reason` populate live.\n\nAlso corrects a pre-existing miscitation: origin stamping is migration `0028_run_origin.sql`, not \"ADR-0028\" (which is the UI-stack ADR).\n\nTracked as git-bug tickets `4ac99c6` / `ea6c947` / `49d65e6` / `32f3837` (all closed).\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}