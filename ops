{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774717,"nonce":"dUAYYhm07TBkH9zc33DKFlPM24E=","title":"refactor(repo): split build contexts (docker/) from deploy modes (deploy/)","message":"\u003e Imported from pull request #31 (MERGED)\n\u003e Branch: `reorg/docker-deploy-modes` → `main`\n\u003e Created: 2026-07-18T18:41:39Z · Merged: 2026-07-18T19:11:14Z\n\n\nReorganize the top-level layout along the **build-vs-deploy** axis. No code changes — pure `git mv` renames plus wiring updates.\n\n```\ndocker/          # BUILD: first-party image contexts\n  server/        ← was ./Dockerfile\n  clone/         ← was deploy/clone   (scarab-clone, ADR-0045)\n  sidecar/       ← was deploy/sidecar (scarab-results-sidecar, ADR-0042)\ndeploy/          # RUN: deployment modes\n  helm/          (unchanged — the distribution chart)\n  local-proc/    ← was dev/          (server = host process, kind for steps, compose pg/minio)\n  local-helm/    ← was deploy/local  (server = Helm-deployed image on colima)\n```\n\n\n- **clone/sidecar are not plugins.** They're trusted, first-party runtime images. ADR-0008 deliberately keeps the OCI registry as the only \"plugin ecosystem\" (zero SDK), so a `plugins/` dir would mislabel our most-trusted components. They group under `docker/` by *what they are* (build contexts), alongside the server image.\n- **`dev/` and `deploy/local` are both local deployment modes**, differing only in *how scarab-server runs* — a host process vs a Helm-deployed image. So they become siblings of `helm/` under `deploy/`, named `local-proc` / `local-helm`.\n\n- `image.yml` — `context`/`dockerfile` for all three images + PR path filters\n- `Justfile` — `up`/`demo`/`down`/`logs`/`serve` paths\n- `up.sh` — repo-root depth (now `$here/../..`, one level deeper) + log-path messages\n- `.dockerignore` / `.gitignore` — runtime-artifact + `.env` secret paths\n- `local-proc` / `local-helm` scripts — internal path refs\n- All READMEs (incl. server image build now needs `-f docker/server/Dockerfile .`)\n\n- Git recorded all 12 moves as **pure renames**\n- All moved shell scripts pass `bash -n`; `image.yml` + compose/kind/postgres YAML parse\n- Every Dockerfile `COPY` still resolves (clone/sidecar copy siblings that moved with them; server context stays `.`)\n- Zero stale `deploy/clone` · `deploy/sidecar` · `deploy/local/` · `dev/` operational references remain (ADR/handoff prose left as historical record)\n\n- No `docker build` of the server image (mechanical move, context unchanged) or live `just up` run\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}