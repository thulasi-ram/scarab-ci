{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774718,"nonce":"DTek4hBdxiZJEbRpUhwqI0vsH5Q=","title":"fix(dashboard): footer beetle rolls continuously","message":"\u003e Imported from pull request #33 (MERGED)\n\u003e Branch: `fix/dashboard-beetle-always-rolls` → `main`\n\u003e Created: 2026-07-18T19:51:57Z · Merged: 2026-07-18T19:52:29Z\n\n\nThe dashboard footer dung-roller now rolls **continuously**, regardless of run state.\n\n\nThe beetle's motion was gated on `rolling() = inFlight() \u003e 0`, so it only animated while a run was `running`/`pending` and parked frozen otherwise. Before the dashboard was de-mocked it appeared to roll constantly (fake in-flight runs); on a real instance it's frozen almost all the time — which reads as \"broken.\"\n\nVerified live against a `just local-helm local` instance: API healthy, all runs terminal (`succeeded`/`dead_lettered`/`cancelled`), `inFlight = 0`, `prefers-reduced-motion` off — beetle correctly parked. So this is a semantics change, not a bug fix in the mechanics.\n\n\n- Always apply the `.rolling` class and pass `playing={true}` to `AsciiScene`.\n- Drop the now-unused `inFlight()` helper and `rolling` memo.\n- Update the CSS comment to describe the always-on behavior.\n- The `prefers-reduced-motion` freeze still applies.\n\n\n`just ui` (or rebuild the helm image) and open the dashboard — the beetle should walk its ball across the footer on a 52s loop with legs/ball spinning, even with nothing in flight.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}