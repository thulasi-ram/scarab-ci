{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1784202256,"nonce":"k26OC85HCcU2lC6aoiZBGWPH1pE=","title":"The canonical `scarab-clone` OCI image (ADR-0045 §Clone image): the trusted image the `clone` step kind runs in, owned and versioned by Scarab rather than the pipeline author (the kind has security invariants an arbitrary image can't guarantee).","message":"Contents: `git`, `git-lfs`, the `GIT_ASKPASS` helper that reads the short-TTL token from a tmpfs file under `/scarab/secrets/` (never token-in-URL/argv), and the pre-snapshot guard that asserts `.git/config` (and `.git/**`) carry no credential before a CAS snapshot is taken.\n\nBuilt and digest-pinned by the same `.github/workflows/image.yml` pipeline as the server. This formally takes on a first-party image pipeline (this image + the ADR-0042 results-egress sidecar, which today points at a phantom `ghcr.io/scarab/sidecar:latest`).\n\n\n- [ ] `deploy/` (or a crate dir) has a Dockerfile for `scarab-clone` with `git` + `git-lfs` present\n- [ ] The `GIT_ASKPASS` helper reads a token from a tmpfs path and produces a credential-free remote URL in `.git/config`\n- [ ] The config-scrub guard exits non-zero if any credential is found under `.git/`\n- [ ] `image.yml` builds and pushes `scarab-clone` with digest pinning, alongside the server image\n- [ ] Image is non-root, minimal, and follows the restricted baseline (ADR-0039)\n\n\nNone - can start immediately","files":null}]}