{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774750,"nonce":"/g6lo3LA/RgNRq3LBLIhqwQj/xo=","title":"feat(depot): a Depot outage delays steps instead of dead-lettering runs (e140121)","message":"\u003e Imported from pull request #107 (MERGED)\n\u003e Branch: `feat/e140121-depot-retry` → `main`\n\u003e Created: 2026-08-26T15:13:48Z · Merged: 2026-08-26T15:24:07Z\n\nCloses git-bug e140121. **Stacked on #106** — after #106 merges, retarget this to main.\n\nThe prerequisite for Depot replicas: a 20–30s Depot outage during Init used to burn all 3 never-started attempts in seconds and dead-letter the run.\n\n- **Timeouts**: WorkspaceClient gets connect 5s / read-idle 30s (control-plane feed client: 2s/10s fast-fail to cold); PUT legs get a size-derived total timeout `max(60s, size/1MiB·s)` — read-idle provably does not bound a stalled body-send.\n- **Retry**: fetch and drain legs retry transient failures (connect/timeout/5xx/429/408 only — HTTP status now carried in the error, permanent 4xx exits immediately) inside ONE shared window `clamp(step_timeout/10, 5s, 60s)`, backoff 1s→10s; the window **arms at the first failure**, so transfer time never consumes the retry budget. Step timeout rides `SCARAB_WORKSPACE_STEP_TIMEOUT_SECS` (same number as activeDeadlineSeconds).\n- **Classification split**: exit 2 = content genuinely gone (404 only) → new forward-only `FailureKind::MissingInputs` (1 attempt, run *Failed* not DeadLettered, cause points at Rerun-widening); exit 3 = auth (cause names the token-exp math); exit 4 = env/skew Config; default/137 arm pinned Infra. **Rollback-breaking**: old binaries cannot replay MissingInputs events — forward-only, documented in the commit.\n- **Drain re-drive**: whole-leg retry; a Depot restart mid-drain re-drives from scratch — proven idempotent (session-unique pack keys, /have on the durable index, ON CONFLICT ledger, write-once record); a lost 2xx surfaces as 409 = success. `classify_drain` Exit(0)-no-record KEEPS FatalConfig (audit showed the transient counterexamples cannot occur within the constants) with a dual-hypothesis cause.\n- Headline test drives the real wsfetch binary against a restarted workspaced router: the fetch delays and completes instead of failing.\n\nChain: design → skeptical audit (9 findings, 4 major) → judge amendments → build → skeptical review (FIX-FIRST, 4 majors incl. the window-arming bug) → fixes → suite 973/973.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}