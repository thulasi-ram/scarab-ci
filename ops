{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774732,"nonce":"6Qim1fNCQDOeRL3WRcoKskxn9GY=","title":"test(web-ui): Playwright tier — boot smoke + rich-fixture run-detail walkthrough (test-strategy Phase 3)","message":"\u003e Imported from pull request #66 (MERGED)\n\u003e Branch: `test/phase3-ui-playwright` → `main`\n\u003e Created: 2026-07-24T04:29:19Z · Merged: 2026-07-24T05:48:48Z\n\nThe browser tier of the test strategy's Phase 3 (see the strategy doc referenced in PR #60). **Stacked on #62** (the vitest tier) — base is `test/phase3-ui-vitest`, merge that first.\n\n\n- **EXACTLY 2 specs**, text/role-based assertions only, **no pixel/screenshot snapshots**, chromium only.\n- No new scenario fixtures beyond what the walkthrough needs: ONE rich run added to the existing acme mock.\n\n\n**Rich fixture** (`src/mock.ts`): one additional run in mock mode with a `RunRerunRequested` boundary (two versions; `build@a1` shadowed, `test@a1` superseded, failed→auto-retried build in the latest window), a shared `postgres` service + docked `redis` sidecar on `test` (ADR-0058), a pending manual `approve` gate (run suspended), and one artifact of record. Typed against the generated OpenAPI schema (type-only imports — the mock-before-App install ordering is unaffected).\n\n**Playwright setup**: `@playwright/test` devDep, `playwright.config.ts` with `webServer` = the `dev:mock` Vite server (`VITE_SCARAB_MOCK=1`) on a pinned strict port, `npm run test:e2e`.\n\n**Spec 1 — boot smoke** (`e2e/boot.spec.ts`): app loads at `/`, inbox + repo list render real fixture content, and any page/console error fails the test — catches the top-level-await-breaks-boot escape class.\n\n**Spec 2 — run-detail walkthrough** (`e2e/run-detail.spec.ts`): DAG step nodes + services lane + docked sidecar chip; version dropdown shows the two versions in canonical glossary language (\"original run\" / \"you reran build\") with superseded ⊘ evidence, plus a page-wide assertion that the words \"Take\"/\"Attempt\" never surface; newest-first activity rail; the gate's pending-approval affordance; the failed→retried step's tries dropdown with per-try cause + outcome; artifacts footer.\n\n\n- `npm ci` + `npm run typecheck` clean\n- `npm test` — the base branch's 72 vitest tests stay green (mock edits don't touch the derivation suites)\n- `npm run test:e2e` — 2/2 green\n- `npm run build` green\n\nNo `.github/` changes — CI wiring follows separately.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}