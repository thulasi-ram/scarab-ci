{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":3,"timestamp":1784531052,"nonce":"JGoLQLRvUdUk+KUEvXdVGiyU4WU=","message":"Both halves implemented on branch `feat/ref-picker-and-run-filters` (commit 0e1223c).\n\nPart A — searchable ref picker:\n- Backend: `GET /v1/repos/{org}/{repo}/refs?q=` added. New `ForgePort::list_refs(repo, query)` + `ForgeRef`/`RefKind` domain types + shared `filter_refs` (case-insensitive substring). Implemented on GitHub (/branches+/tags, paginated), Forgejo (paged, commit.id|sha), RegistryForge delegate, and FakeForge. Response `RefsResponse{refs:[RefDto{kind,name,short_sha}]}`, branches-before-tags name-sorted. Contract suite extended (known_branch). OpenAPI + web-ui schema regenerated.\n- Frontend: RunPipeline free-text ref input replaced with a live-filtered combobox (keyboard nav, branch/tag tag per row, short SHA, 'type a full SHA to pin' hint). Degrades to plain free-text when no forge (listRefs returns []).\n\nPart B — RepoView Runs filters: client-side author / branch / PR-number dropdowns over the already-fetched rows, populated from distinct present values, combined with the status pills, + a clear button. Sparse origin fields honored (a disengaged filter never hides a row; the PR dropdown hides entirely when no run carries a PR).\n\nVerification:\n- cargo test --workspace green (442 passed, 17 live-gated ignored); web-ui typecheck + build clean.\n- LIVE against real GitHub (thulasi-ram/scarab-ci via the dogfood GitHub App on colima): /refs returns the 3 real branches sorted; ?q=dogfood, ?q=MAIN (case-insensitive), ?q=e2e (mid-name), ?q=xyzzy (empty) all correct.\n- UI verified in-browser: combobox filters live; found+fixed a dropdown-clipping bug (`.panel` overflow:hidden clipped the menu — added `.panel-combo` overflow:visible with header re-rounded). Part B filter bar renders with author/branch dropdowns; PR dropdown correctly hidden (no PR runs).\n\nOpen: not yet merged (single PR for both per request). Deployed dogfood image predates the one-line CSS clip fix (fix verified via vite dev against the live backend); a redeploy would fold it in. No tags exist on the repo, so the tag path wasn't exercised live.","files":null}]}