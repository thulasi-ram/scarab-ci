{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774730,"nonce":"kOVCLPLGEmLKII/T4wpIGo0KQ2k=","title":"ci(test): run the suite in CI — nextest + PG service + coverage ratchet (test-strategy Phase 0)","message":"\u003e Imported from pull request #61 (MERGED)\n\u003e Branch: `test/phase0-ci-wiring` → `main`\n\u003e Created: 2026-07-24T03:26:56Z · Merged: 2026-07-24T05:47:59Z\n\nPhase 0 of the test strategy (docs/audits/2026-07-24-test-strategy.md, PR #60): make the existing suite actually run and gate merges. CI previously ran only openapi-drift — zero tests.\n\n\n- **`test` job**: `cargo nextest run --workspace` on ubuntu-latest against a `postgres:16` service (same triggers as openapi-drift; Swatinem/rust-cache, taiki-e/install-action).\n- **`coverage` job** (sibling, so the gate stays fast): `cargo llvm-cov nextest` (excludes scarab-testkit + `tests/` harness code), lcov artifact, per-crate line-coverage table in the step summary, and a **ratchet**: `scripts/coverage_ratchet.py` fails the job if any crate drops \u003e0.5pp below `docs/audits/coverage-baseline.toml`. The committed baseline is a marked **placeholder (all 0.0)** — regenerate with `just coverage` and commit deliberately.\n- **Loud skips**: the three `fresh_db()` harnesses (scarab-server / scarab-db-postgres / scarab-secrets-postgres) now shout on stderr when `SCARAB_TEST_DATABASE_URL` is unset, and **panic** under `SCARAB_TEST_REQUIRE_PG=1` (set in CI) — the suite can never silently lose its PG-backed tests again.\n- **`just test`**: brings up the compose Postgres on demand (reuses anything already on 55432), exports the URL, runs nextest with a `cargo test` fallback. New **`just coverage`** mirrors the CI coverage job and regenerates the baseline.\n- Deleted the empty `rootless_buildkit_builds_an_image` placeholder test (asserted nothing).\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}