{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":3,"timestamp":1785650709,"nonce":"w3TvSccyFf2SKCS/+8WnvcCTpQ8=","message":"**Closed 2026-08-02 — the tier executed for the first time, on kernel 6.8.0-117 (colima), 2 passed / 0 failed.**\n\n`just overlay-tests-colima` runs `changeset_overlay.rs` in a privileged container on the colima node (Linux + CAP_SYS_ADMIN + a real-disk upperdir is all it needs; cargo runs inside the VM because this host has no aarch64-linux cross toolchain). Cold ~40 min on 2 vCPU, rerun 39 s.\n\nAll four change-set forms pass, as an EXACT set rather than a subset: new file, copy-up modify, deletion→whiteout, directory rename→`trusted.overlay.redirect` reported as a graft. Also covered: opaque dir, symlink, nested-write ancestors, added empty dir, and that a FILE rename produces no redirect.\n\n**The test was correct as written** — its `Measured` block had been hand-derived from a manual container session and had never been checked against the code; it held on first execution.\n\n**Still unexecuted, and worth its own note:** the kernel took the `redirect_dir` branch (live on 6.8.0-117 despite the module default `N`), so the synthesised-marker fallback `set_overlay_redirect` is now the only path in this file that has never run.\n\nNegative controls, same binary: opted-out `--ignored` → 2 failed; opted-in but unprivileged → 2 failed. The tier is loud, not silently green.","files":null}]}