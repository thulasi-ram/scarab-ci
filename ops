{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774731,"nonce":"Sew30vywQsvycr6gqAFaaEKyvNo=","title":"test(web-ui): extract run-detail derivations + no-DOM vitest tier (test-strategy Phase 3)","message":"\u003e Imported from pull request #62 (MERGED)\n\u003e Branch: `test/phase3-ui-vitest` → `main`\n\u003e Created: 2026-07-24T03:34:13Z · Merged: 2026-07-24T05:48:08Z\n\n\nThe no-DOM tier of **Phase 3 (UI)** of the test strategy (`docs/audits/2026-07-24-test-strategy.md`, follows PR #60): pure-derivation extraction + Vitest, nothing else.\n\n- `src/dag-layout.ts` — longest-path layering + cycle guard, out of `Dag.tsx`\n- `src/attempts.ts` — take-window attempt scoping, scoped/of-record try resolution, `FilmstripTry` + `tryOutcome`/`tryTone`/`causeSuffix` mappers, `stripTries` (out of `StepPane`/`RunDetail`/`AttemptsDropdown`)\n- `src/logs.ts` — `levelOf` log-line classifier (out of `StepPane`)\n- `src/takes.ts` grows `attemptN`, `stepTiming`, `visibleArtifacts`, `bucketOf`/`tally`, version-row helpers + `versionRows` (out of `RunDetail`)\n\nComponents keep thin wrappers over signals/props.\n\n`deriveTakes` boundaries incl. rerun-while-running · `replayTake` superseded/not_run/straddling-running + per-take frontier/windowAttempts · `attemptCauses` initial/auto-retry/rerun/cascade + readopted/superseded/shadowed · `ofRecordAttemptId` · take-windowed `stepTiming` · `visibleArtifacts` horizon + of-record recompute · `versionRows` copy/tallies/selection · dag layering incl. cycle guard · attempt scoping · dropdown mappers · `events.ts` categorisation · `levelOf`.\n\nFixtures follow `takes.ts` event shapes and `mock.ts`'s `eventsBody` (`src/event-fixtures.ts`, test-only).\n\n`levelOf` never classified real Rust panic lines (\"thread 'main' panicked at …\") as `err` — `\\bpanic\\b` doesn't match \"panicked\". Regex now accepts `panic(ked)`.\n\n`npm run typecheck` ✓ · `npm test` ✓ (72/72) · `npm run build` ✓\n\nCI wiring (`.github/`) and the Playwright/DOM tier follow separately to avoid conflicts.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}