{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774733,"nonce":"2nIl971XUCl9z/fsqLNfeWihc0Y=","title":"test(e2e): scarab-e2e cross-layer suite — crash/resume at stack grain (test-strategy Phase 2)","message":"\u003e Imported from pull request #68 (MERGED)\n\u003e Branch: `test/phase2-e2e` → `main`\n\u003e Created: 2026-07-24T04:54:45Z · Merged: 2026-07-24T05:49:07Z\n\nImplements the **full-stack E2E tier** of Phase 2 of the test strategy\n(docs/audits/2026-07-24-test-strategy.md, PR #60). **Stacked on #61**\n(`test/phase0-ci-wiring`) — merge that first; this PR deliberately targets it\nto avoid Justfile conflicts.\n\n\nNew workspace crate **`crates/scarab-e2e`**: an integration-test-only pure\nHTTP driver (reqwest + hand-rolled minimal DTOs — no generated client) over a\nRUNNING `deploy/local-proc/` stack. Every test is gated on `SCARAB_E2E=1` and\nskips **loudly** otherwise, so plain `just test` / workspace runs stay\ncluster-free. **Zero auto-retries** — a red run is triaged as a real bug.\n\n\n| # | scenario | status |\n|---|----------|--------|\n| 1 | `happy_path` — dispatch through the **`scarab` CLI binary** against a seeded repo (ForgeConnection + repo binding + `_forge` credential; `base_url` at an in-test fake Forgejo serving the pipeline YAML at a pinned SHA) → kind Pod → `succeeded`, logs non-empty | ✅ passed live |\n| 2 | `crash_resume` — **THE WEDGE**: spawns its OWN `scarab-server` (throwaway DB carved from the stack PG via the maintenance-URL pattern, own port + object dir, same kind cluster), **SIGKILLs it mid-step**, respawns, asserts exactly-once: 1 `AttemptStarted` per step, an `AttemptReadopted` (re-adoption, not re-execution), exactly one terminal transition, both steps' logs present | ✅ passed live (154s) |\n| 3 | `cancel_mid_run` — POST cancel → durable `cancelled` AND the step Pod disappears from the kind namespace (kubectl on the isolated kubeconfig) | ✅ passed live |\n| 4 | `rerun_takes` — one `RunRerunRequested` take boundary targeting the pressed step, a second **distinct** `AttemptStarted`, attempts==2, rerun output in the replayed logs | ✅ passed live |\n| 5 | webhook → run → status posted back | `#[ignore]` stub |\n| 6 | CAS-loss restart fails fast (helm-dogfood escape) | `#[ignore]` stub (would pin the broken behaviour today) |\n\n\n- **`just e2e`** owns the stack (CLAUDE.md: recipes are canonical): `up.sh` →\n  build server+CLI → `cargo nextest run -p scarab-e2e --no-fail-fast` →\n  `down.sh` even on red (exit code preserved; server log tailed first).\n- **`.github/workflows/e2e.yml`** — nightly cron + `workflow_dispatch`,\n  ubuntu runner: free-disk step, `CARGO_PROFILE_DEV_DEBUG=0`, kind via\n  `helm/kind-action` (`install_only`), then exactly `just e2e`.\n- Harness: `up.sh` now derives its health probe from `SCARAB_ADDR` (rebind\n  via `.env` when :8080 is taken) and reuses a Postgres already serving\n  55432 (the same tolerance `just test` has).\n\n\nAll four real scenarios ran green against a live stack (compose PG+MinIO +\nkind on colima docker + host-process server):\n\n```\nPASS [  62.4s] happy_path_cli_dispatch_to_succeeded_with_logs\nPASS [ 154.5s] crash_resume_completes_exactly_once\nPASS [  36.9s] cancel_mid_run_tears_down_the_pod\nPASS [ 122.2s] rerun_creates_a_new_take_with_fresh_attempt_evidence\n```\n\nThe crash/resume run was observed end-to-end on the cluster: SIGKILL landed\nmid-`sleep 20`, the respawned instance reclaimed the outbox lease, emitted\n`AttemptReadopted` (no second launch), settled step 1 and ran step 2.\n\nTwo real bugs the live runs shook out of the suite itself (fixed in-branch):\na single-threaded tokio runtime deadlocking the fake forge against the\nblocking CLI spawn, and busybox needing the self-service `run_as_root` grant\nunder the ADR-0039 hardened baseline.\n\nWithout `SCARAB_E2E=1` the whole crate skips loudly (verified: 4 eprintln\nskips + 2 ignored under a plain nextest run).\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}