{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774712,"nonce":"04R328K6lp2gIL19SnV3QUevc6Q=","title":"feat(brand): ASCII beetle scenes + real emblem wired into docs \u0026 web-ui","message":"\u003e Imported from pull request #18 (MERGED)\n\u003e Branch: `feat/brand-ascii-motifs` → `main`\n\u003e Created: 2026-07-17T07:33:09Z · Merged: 2026-07-17T11:55:30Z\n\n\nA new animated decoration tier above the doodles (**DESIGN.md §5**): brand beetles *doing things*, baked offline to ASCII text frames and committed — the apps ship **zero rendering code** and play the scenes by swapping `textContent` at 12 fps.\n\n- Same philosophy as `gen-doodles.mjs`: generate offline (`npm run bake`), commit the output, deterministic reruns.\n- Scenes: **wing-spread + nimbus** (hero + small) and **dung-roller** — parametric rebuilds at the traced emblem's measured proportions. The traced dark layer is one winding-linked compound path (holes by winding), so it can't be dismembered for articulation; it bakes verbatim only as the **static mark**.\n- Format: three text layers per frame split by brand role (emerald / gold / gray), colored via theme tokens at runtime → both themes come free.\n\n- Starlight logo → the real brand emblem (ink-outline on light, gold-outline on dark).\n- Landing hero → the wing-spread loop (frame 0 rendered at build, present at first paint).\n- Dung-roller accent beside *Why now*. **Landing page only** — inner pages keep static doodles.\n- Placeholder `logo.svg` removed.\n\n- Topbar `bug` icon → theme-aware emblem.\n- All-clear state → the wing-spread scene (state moments only — nothing animates behind live data).\n- Dashboard `boxes` doodle → faint static traced-emblem ASCII watermark.\n\nAll players honor `prefers-reduced-motion` (hold an open frame) and skip work while the tab is hidden.\n\n- `scarab-docs-ui`: build green; light + dark visually checked (hero, accent, logo).\n- `scarab-web-ui`: typecheck + build green; light + dark visually checked (topbar, all-clear, watermark).\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\n---\n\n\n**@thulasi-ram** — 2026-07-17T07:46:58Z\n\nUpdated per review: the wing-spread left the all-clear banner (shield icon restored). The dung beetle now walks its ball across the **full viewport at the bottom of the dashboard** on a dashed copper ground line — it rolls while runs are in flight and **parks beside the ball when the dashboard is quiet**, so motion literally means \"something is working\". New baked variant `dungroller-bare` (CSS carries the travel, frames carry the legs + ball spin); also fixed scene-box sizing so a narrow text layer can't clip a wider one.\n\n**@thulasi-ram** — 2026-07-17T08:19:21Z\n\nRound 2 per review: docs hero is now the **square-cropped brand emblem (1:1)** with light/dark variants (the ASCII wingspread is retired from the hero but the scene code stays); **Departure Mono** (OFL, vendored in ui/brand/fonts) is the new display voice — docs h1s/wordmark + web-ui display token — normalized to its single weight; web-ui doodles moved from bottom-right to top-right per the new placement rules.\n\n**@thulasi-ram** — 2026-07-17T09:29:34Z\n\nDesign-language round: **10 Pixel** locked in as the display face (wordmark stays Space Grotesk — the brand name is an identity, not a rendering); **doodles are now dotted** (zero-length dash + round caps) instead of rough.js-sketched, dropping the roughjs dep from both apps; **corner rivets removed** everywhere; **radii sharpened** (5/3/7px web-ui, 0.375rem docs). DESIGN.md rewritten to make the dotted language the single signature: dot-grid + dotted doodles + pixel display face + ASCII beetles.","files":null}]}