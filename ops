{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774746,"nonce":"mwInXoIeyPDNRbeGpH/2/uKTDQw=","title":"Replica-safe drains: committed-marker staging, linger seal, coded retry (afb13c2)","message":"\u003e Imported from pull request #97 (MERGED)\n\u003e Branch: `feat/afb13c2-replica-safe-drains` → `main`\n\u003e Created: 2026-08-24T21:08:29Z · Merged: 2026-08-25T05:28:33Z\n\nMakes the Depot's drain WRITE path replica-safe — the follow-up ADR-0067's implementation notes promised (git-bug `afb13c2`), closing the last replica-local state. Stacked on #95 (sibling of #96; migration numbers coordinated: 0046 here, 0047 there).\n\n**Process:** grounded design (2 candidates + hybrid) → skeptical audit (5 amendments) → independent judge (2 auditor claims corrected, GO) → 4 build slices → suite run → judge post-build verification (6 invariants CONFIRMED) → touch-ups. **995/995 green** (real PG + MinIO).\n\n**Design shipped (B — remove the state, don't route to it):**\n- `depot_packs.committed` (migration 0046): sealed packs stage their index rows immediately (per-pack bytes-before-pointers), visible only to their own fence until the drain-record transaction flips them — `committed OR fence_key = caller` at every durable-presence read, so no other fence can dedup against never-committed staging.\n- The commit doc is built from the row union across replicas ∪ this POST's seals; error records never flip, so `committed=FALSE` + expired residue + no record is exactly the future reclaimer's discriminator (noted in code for `ad79c90`).\n- 2 s injectable linger ticker seals idle tail packs; wsfetch retries only the machine-readable `drain_state_incomplete` 422 (latency-only — the outer re-drive remains the correctness path).\n- Closure validation trees are warm-OR-index (a scattered tree no longer 422s the POST replica).\n- `022aec8` folded in: session tombstones in both sweep and ticker — a late append errors loudly instead of vanishing.\n\n**Tests:** per-pack ordering (65 MiB oversized member, linger off), scattered-blob drain, all-PUTs-elsewhere drain through a cold replica (pins the tree walk), restart-lost-session still refuses a hollow success.\n\n**Honest limit (stated in the chart + values.yaml):** exercised at the acceptance-test grain — two routers, one Postgres, one bucket — not yet on a live multi-replica cluster. `afb13c2` stays open until that run happens.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}