{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774751,"nonce":"XMttNbNSfWo2TFBPd2LePAIWwcQ=","title":"fix(depot-expiry): per-profile cutoffs in nomination end window starvation (a543fef)","message":"\u003e Imported from pull request #108 (MERGED)\n\u003e Branch: `fix/a543fef-expiry-starvation` → `main`\n\u003e Created: 2026-08-26T15:14:21Z · Merged: 2026-08-26T15:24:19Z\n\nCloses git-bug a543fef. Merge after #106 (needs its warm_eviction compile fix on main for a green gate).\n\nThe committed-expiry pass nominated under the LOOSEST retention cutoff and skipped in code — ≥100 old long-TTL runs could permanently occupy the 100-row window and silently stop expiry. Now the per-profile cutoff is a parameterized CASE bound into the nomination SQL (NULL/unknown profiles fall to the default, exactly matching the Rust resolution), and the pre-epoch floor gate folds into the same WHERE. Blockers are never fetched at all.\n\n- `scarab_depot_expiry_candidates_skipped_total` — drop-guard-recorded on EVERY exit path (incl. 40P01 and hard errors), so \"window full of undeletable candidates\" is distinguishable from \"nothing eligible\".\n- Migration 0050: btree on `runs(created_at)` for the reachability floor's every-300s EXISTS (was a full scan; lock tradeoff documented in the file).\n- Starvation test: EXPIRY_BATCH+20 long-TTL blockers ahead of one expired victim; fails on the old code.\n- Residual (borrower-gated candidates can still occupy the window) is deliberately out of scope → git-bug 5cde838.\n\nReview: MERGE-READY, no blockers (injection, CASE-equivalence, floor-lemma scope, and test non-vacuity all verified); touch-ups applied. Suite: 954/954.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}