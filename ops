{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1784231813,"nonce":"aF867a+vjNzvEAlKzIUOER11QYY=","title":"Mint and inject a per-run OIDC token into steps (ADR-0015 keyless federation). The Rs256Issuer exists and Claims::run_subject is defined, but issue() is only called in tests and the driver is spawned with no issuer. Wire: on step launch, mint a token with the run's subject (fork-PR runs get the downgraded \"none\" subject per fork_policy), deliver it to the Pod as a tmpfs file under /scarab/ (ADR-0008 convention, not env), and require a persistent issuer key (ties to ADR-0048: OIDC enabled =\u003e persistent key or refuse boot).","message":"- [ ] Per-run OIDC token minted with the correct subject/audience at step launch\n- [ ] Fork-PR runs receive the downgraded subject\n- [ ] Token delivered via tmpfs file (not env, not argv); absent when OIDC disabled\n- [ ] Issuer uses a persistent key (no per-boot regeneration); JWKS stable across restart\n- [ ] Test: a step can exchange the token and it verifies against JWKS\n\nNone - can start immediately","files":null}]}