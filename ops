{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774752,"nonce":"7sdKonjkwMMQGiJ2QO/0poBUBQg=","title":"fix(depot-expiry): advisory borrower prefilter keeps the nomination window useful (5cde838)","message":"\u003e Imported from pull request #111 (MERGED)\n\u003e Branch: `fix/5cde838-borrower-prefilter` → `main`\n\u003e Created: 2026-08-26T15:50:45Z · Merged: 2026-08-26T18:48:51Z\n\nCloses git-bug 5cde838 (the residual from a543fef / PR #108).\n\nBorrowed fences pass the nomination SQL but are declined inside the victim txn — ≥100 of them would re-occupy the earliest-posted_at window every pass and re-starve expiry. Both nomination arms (recorded + recordless — borrowers on recordless owners are real: pre-epoch owner, residue-swept record, post-epoch borrower deduping against its pack) now carry an ADVISORY borrower `NOT EXISTS`, character-identical to the authoritative in-txn check (same `depot_drain_records` liveness join, same owner correlation). The in-txn check under `FOR UPDATE` remains the only authority; the prefilter only keeps the window useful. No new binds — the CASE/floor parameter numbering is untouched; both join hops are index-backed (0048's `owner_fence` index + the records PK).\n\nTests (all executed, suite 980/980):\n- `a_window_of_borrowed_blockers_cannot_starve_a_victim_behind_them` — EXPIRY_BATCH+20 borrowed blockers ahead of one expired victim; kills a dropped-prefilter mutation.\n- `a_borrower_arriving_inside_the_victim_txn_is_caught_and_counted` — constructs the prefilter→txn race via the existing `#[cfg(test)]` in-txn injection hook (lands after the FOR UPDATE); kills a dropped-in-txn-check mutation and pins the skipped counter.\n- `a_dangling_edge_without_a_borrower_record_does_not_hide_the_owner` — the dangerous direction: an edge-alone (stricter-than-authority) prefilter would hide this owner forever; asserts it still expires.\n\nReview: MERGE-READY; its one LOW (the dangling-edge coverage gap + a false mutation claim in a comment) is fixed in the second commit.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}