{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774731,"nonce":"xPjaQIkfEUsUiSzP8C7L2yd+uGM=","title":"test(server): field-preservation, restart-alias pin, QA-bug regressions (test-strategy Phase 1)","message":"\u003e Imported from pull request #63 (MERGED)\n\u003e Branch: `test/phase1-functional` → `main`\n\u003e Created: 2026-07-24T03:42:29Z · Merged: 2026-07-24T05:48:18Z\n\nPhase 1 (backend functional gaps) of the test strategy in\n`docs/audits/2026-07-24-test-strategy.md` (follow-on to PR #60). All tests run\nthe real axum router + real engine over `scarab-testkit` fakes (InMemoryDb,\nFakeExecutor, FakeAuthenticator) — classical style, asserting observable\ndurable state.\n\n\n**1. API→StepSpec field preservation** — `tests/spec_preservation.rs`\nTable-driven: POST /v1/runs setting every field the inline payload supports\n(env, secrets, `security.run_as_root`, timeout, placement_profiles, resources,\nsidecar services), one scheduler tick, assert the spec the FakeExecutor\nreceived carries each field (the class of the escaped `run_as_root` drop).\n`uses:` pinned on the durable spec; governed grants / `k8s_overlay` pinned\nrejected fail-closed.\n\n**Live bug found \u0026 fixed**: `create_run` persisted the run, IR, params, and\nearlier steps *before* admitting each step's privilege grants — a multi-step\nrequest rejected at a later step (400) left a half-created run whose\nalready-persisted steps the scheduler would execute. Now admits/translates\nevery step first, then persists; a rejected request creates nothing.\nRed-first: `rejected_create_run_persists_nothing` failed pre-fix with\n\"a rejected create_run must persist NOTHING — found 1 run(s)\".\n\n**2. Restart-alias pin** — `tests/restart_alias.rs`\nDeprecated `POST .../steps/{step}/restart` behaves identically to `/rerun`:\nsame 202, same `RunRerunRequested` Take fork (target/invalidated/actor), same\nre-armed step + reopened run; 404 parity on an unknown run.\n\n**3. QA-sweep regression pins** — `tests/qa_regressions.rs`\nBoth 2026-07-23 QA bugs turned out to be **already fixed on main** in a1a009b\n(with engine-level tests), so no new fix — these pin the HTTP surface:\n- approve on a SKIPPED manual gate → 409, no phantom `GateApproved` (was a\n  lying 202); non-gate step 409 vs unknown step 404.\n- operator cancel via FakeAuthenticator → exactly one\n  `RunCancelRequested { by: Some(\"alice\") }` on the durable log.\n\nRed verified by temporarily re-introducing each bug and watching the pins fail\n(evidence lines in the commit message).\n\n\n- `cargo check --workspace` clean\n- `cargo test --workspace`: 543 passed, 17 ignored (no Postgres needed for the\n  new suites)\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}