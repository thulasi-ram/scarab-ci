{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774753,"nonce":"szYttBVnBkwKG4rSxW/04So1Zn0=","title":"feat(rerun): the affordance names the whole cascade, not its start point (4afaa3e)","message":"\u003e Imported from pull request #113 (MERGED)\n\u003e Branch: `feat/4afaa3e-cascade-disclosure` → `main`\n\u003e Created: 2026-08-26T16:39:27Z · Merged: 2026-08-26T18:49:13Z\n\nCloses git-bug 4afaa3e. Disclosure, not prevention — ADR-0027's \"smart never means mysterious\" applied to blast radius: retrying `push` with expired inputs re-runs `deploy-staging`, and the human must be told before they click.\n\n- **One computation**: `plan_rerun_over` (already shared by preview and both executions) now derives execution-ordered `steps[]` with a reason per member (Target | Cascade | Regenerate | RegenerateCascade), `because_of` (min in-set need, deterministic), and `is_gate`. GET preview and POST rerun/retry/`/restart` all serialize through one mapping; the POSTs return the **executed** plan (202 body, additive), and the UI toasts if it diverged from the preview.\n- **UI**: confirm popover on Rerun and Retry — \"This re-runs `push`.\" / \"This re-runs from `build`, which also re-runs `deploy-staging`.\" Gates never claimed to run: \"pauses for approval at `X`\". Retention named generically (a day-count from the flat knob can be wrong for profiled runs). Plans fetched on live runs too; stale-plan-under-a-fresh-target fixed (review MEDIUM).\n- **Two real oracle bugs fixed on the way** (audit HIGHs): the widening oracle's tiered handle used `fall_through_on_warm_error`, so a Depot outage laundered into a definitive NotFound and could present a full re-run-from-clone plan as \"expired\" — it now uses a strict handle where outage = error = never-widen (counterfactual test proves the old handle widens); the same fix applied to the cache-key oracle, whose false-absence would corrupt the cache key.\n- Expired-vs-evicted: one message — eviction cannot widen while the Depot answers (warm misses resolve through the pack index); the oracle measures \"materialisable now\", documented.\n\nChain: design → audit (10 findings, 2 HIGH) → judge → build → review (FIX-FIRST: the stale-plan popover) → fixes. Suites: **985/985 Rust, 123/123 UI**; openapi/schema regenerated, drift-clean.\n\nOpen owner decision (review finding, non-blocking): the reasons list lives only in the ephemeral 202 body — `RunRerunRequested` events cannot reconstruct what the user was told. Say the word and I file the event-enrichment ticket.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}