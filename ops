{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774708,"nonce":"I0ZJJHlOod1WkuH0ThoylQMb8R8=","title":"docs: documentation site — Astro Starlight (ADR-0040)","message":"\u003e Imported from pull request #5 (MERGED)\n\u003e Branch: `feat/adr-0040-docs-site` → `main`\n\u003e Created: 2026-07-14T15:38:56Z · Merged: 2026-07-14T15:40:29Z\n\nImplements **ADR-0040** — the Scarab documentation site. Decided via a grill-me design session; ADR + handoff included.\n\n\n1. **docs(adr)** — ADR-0040 + build handoff (Astro Starlight over Docusaurus/mdBook, in-repo, GitHub Pages, DESIGN.md-branded).\n2. **refactor(ui)** — `ui/` promoted to a container (like `crates/`); the SolidJS app moves to `ui/scarab-web-ui/` to make room for `ui/scarab-docs-ui/`. Mechanical path fixups (gen script `../../openapi.json`, `Justfile`, `.gitignore`).\n3. **feat(docs)** — the Starlight site + the rough.js doodle system.\n4. **ci(docs)** — GitHub Pages workflow, release-gated (`v*` tags + manual dispatch).\n\n\n- **Astro Starlight**, not Docusaurus — Vite-native (matches the UI stack), zero-config offline search, lighter; versioning deferred.\n- **In-repo, sources canonical content in place** — API reference from `../../openapi.json` (starlight-openapi); ADRs + `CONTEXT.md` synced from `../../docs` by a **gitignored prebuild step** (H1 → frontmatter title). Single source of truth stays in `docs/`; nothing committed twice.\n- **Operator-primary IA, published honestly** — *Run locally* is real today; *Deploy with Helm*, *Guides*, *Configure* are explicit `stub`/`wip` pages. The Helm chart itself is out of scope (product work).\n- **Branding = `docs/DESIGN.md` tuned for docs** — carapace-green ladder, emerald accent, copper outline-only, Inter + JetBrains Mono, zero shadows; OS-respecting light + dark; screenshots framed as cards so a dark shot blends in either theme.\n- **Doodles throughout** via a standalone **offline generator** (`scripts/gen-doodles.mjs`): Lucide icons → rough.js **once** (DOM-free, fixed seed) → committed static SVGs. The site has no rough.js at build or runtime. Motion (parallax + stroke draw-on) is best-effort, `prefers-reduced-motion`-gated. **No Lottie.**\n\n\nBuild green (51 pages). Verified in-browser: landing renders in both themes (dark = the carapace signature, screenshots blend), full sidebar IA with badges, ADR pages (H1→title, TOC), API reference from the spec, Pagefind search. Local: `just docs`.\n\n\nEnable Pages (Settings → Pages → Source: **GitHub Actions**), then run the `docs` workflow via **Actions → Run workflow** (no `v*` tag exists yet, so nothing auto-publishes until a release is tagged).\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}