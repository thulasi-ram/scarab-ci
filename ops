{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774728,"nonce":"DgQ4LAkHsuGuqdrP9S7BcjXo5FQ=","title":"Run detail: rerun/retry, services-in-DAG, and blueprint-first layout (ADR-0056/0058)","message":"\u003e Imported from pull request #56 (MERGED)\n\u003e Branch: `fix/run-budget-active-time-only` → `main`\n\u003e Created: 2026-07-23T16:34:56Z · Merged: 2026-07-23T16:45:25Z\n\n\nThis branch started as a run-budget fix and grew into a broad run-detail + engine pass. It's one PR by request; grouped by theme below.\n\n- **Budget bills active (Running) time, not wall-clock** (`b774c7a`) — queued / idle / gate-suspended time used to count, so reruns re-failed on the clock alone.\n- Settle runs blocked on dead-upstream gates; per-Take budget (`fcc5d27`).\n- Classify permanent config rejections as a fail-fast verdict (`ebbb41d`).\n\n- **Rerun** forks a new version of the run from a step (manual; works on any terminal *or* running target — a running target's in-flight attempt is **voided → superseded**). **Retry** gives a *failed* step another attempt in the *same* version. A **FAILED prerequisite blocks both**; only succeeded/skipped prerequisites permit them.\n- Superseded is intrinsic-terminal; recorded a real per-attempt `AttemptOutcome` (running / succeeded / failed / superseded / cancelled). Fixes: superseded \u0026 cancelled no longer clobbered by the teardown-induced `Lost` verdict; deterministic attempt ordering; `record_attempt` never downgrades recorded evidence; supersede/cancel teardown retries instead of orphaning the Pod.\n- Naming: **keep `retry`** (it's also the auto-retry policy key); rename **restart→rerun** everywhere (`RunRerunRequested` serde-aliased for old logs; `RestartError` → `RerunError`).\n- FE: per-try outcome now renders from `AttemptDto.outcome` (running/superseded no longer show green); Retry button; failed-prerequisite blocking + tooltips.\n\n- Attempts **dropdown** (replaced the filmstrip); DAG reduced to a **pure status map**; evidence **coordinate stamp** (`step · try N · outcome · version`); of-record Outputs vs per-try Results; superseded / not-run display states.\n\n- **Backend**: per-sidecar step **log capture** — a sidecar's stdout is captured as its own stream, keyed via a synthetic `{step}::service-{i}` id (no DB migration; reuses the shared-service log path), read via `?sidecar=\u003cindex\u003e` on the existing step-logs route (no new route). `StepStatusDto` gains `services` + `uses`.\n- **FE**: shared services render as **peer nodes** + dotted `uses` edges; **docked sidecar chips** on the step + a per-container Logs selector; a `ServicePane` for a selected service; retired the beside-DAG services panel.\n\n- One slot: **Cancel run** while active ↔ **New run** when terminal — removes the perpetually-greyed Cancel.\n\n- Stack: full-width **left→right** DAG band on top (**max-height + dynamic height + fit-to-view zoom + pan**), evidence **pinned full-width below** (always visible; no reorientation on select), versions folded into a toolbar **dropdown**. Design mockup (A vs B + rejected options) in `docs/design_artifacts/dag-layout-redesign.html`.\n\n- `cargo check` / `cargo test --workspace` green; FE `typecheck` + `build` green.\n- **Live on colima (helm)**: rerun-while-running → superseded + Pod torn down; cancel-while-running → cancelled + Pod torn down; retry a failed step (new attempt, same version); 409 on a failed prerequisite; services-in-DAG on the real dogfood pipeline (postgres peer node `ready`, redis sidecar logs streaming via `?sidecar=`); contextual Cancel/New-run across the active→terminal transition; and the Proposal-B layout (whole pipeline fits via fit-zoom, evidence pinned below, no reorient on select).\n\n- dogfood `version` + `test` legs fail on **Environment(`production`) governance**: `run_as_root` isn't granted for the alpine digest (`version` → `CreateContainerConfigError`), and the `test` legs fail **pre-Pod** with no log line. Pre-existing and orthogonal to this PR — but the silent pre-Pod rejection is an engine **observability gap** worth a follow-up (log the launch/admission error + stamp a failure reason on the attempt).\n- Helm-local uses CAS on an `emptyDir` (no S3), so reruns of pre-restart runs hang — a known local-dev limitation.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}