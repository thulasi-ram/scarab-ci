{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774745,"nonce":"F9pLzO4/SHtrxe4PTwBafGbT5UA=","title":"ADR-0067: the pack is the record — mandatory Postgres+S3, one-pass durable drains, packs","message":"\u003e Imported from pull request #95 (MERGED)\n\u003e Branch: `feat/adr-0067-pack-is-the-record` → `main`\n\u003e Created: 2026-08-24T05:46:27Z · Merged: 2026-08-25T05:28:14Z\n\nImplements ADR-0067 end to end, stacked on #93. Everything below ran through the full pipeline: plan → 5 implementation slices → skeptical reviewer + auditor (20 findings) → independent judge (verdicts per finding) → fixes → judge re-verification → **992/992 tests green** (real PG + real MinIO on the wire).\n\n\n**ADR-0067 \"The pack is the record\"** (+ dated amendment pointers in 0061/0063/0064/0065/0066, CONTEXT.md updated):\n\n- **Postgres and an object store are hard requirements.** The Depot boots fail-closed (write-and-delete cold probe); no more silent `LocalDir` default; the workspace role connects to Postgres but **never migrates** (fence-token auth carve-out — no OAuth, no KEK).\n- **One-pass durable write.** The drain streams pod → Depot → bucket as size-capped **packs** (`packs/\u003cfence\u003e/\u003csession\u003e-\u003cseq\u003e.pack`, footer index + trailer, commit pack written last). The deferred flush RPC, `DurabilityTier`/WarmOnly, and `attempts.output_durability` are deleted — a green drain *is* durable.\n- **The pod labels each upload** `durable` / `cache-only`; only the pruned output closure goes durable (absent header = durable for old-wsfetch compat; fence*less* durable is refused).\n- **Replica-correct records:** drain records + the per-fence write ledger moved from the Depot's local disk to Postgres (migrations 0043/0044/0045); `/have` answers the durable index (+ additive `missing_warm`), fixing the cross-replica dedup lie.\n- **Algorithm-tagged addresses** (`sha256:\u003chex\u003e`) accepted on every wire surface; pack footers and index rows are born tagged (ADR-0067 part 12; BLAKE3 stays a tagged-in follow-up).\n- CI runs MinIO everywhere the touched crates test; the kind live tier moved off local-dir; `just test` wires compose PG+MinIO (dedicated `scarab-test` bucket).\n\n\nConfirmed-and-**fixed** in this PR: success-record-with-no-packs restart window (422 gate on durable closure presence); pack-key overwrite class (session-unique keys); fenceless \"durable\" mislabeling (4-arm header matrix); pack trailer short-read panic; false replica-safety and flush-era prose throughout. Refuted: 1 (loose fallthrough exists at the caller).\n\nConfirmed-and-**ticketed** (git-bug): `afb13c2` **drain write path is still replica-local — `replicaCount\u003e1` remains unsafe for drains** (the chart now says so honestly); `ad79c90` pack reclaimer + multipart abort hygiene (packs are never deleted yet — errs safe); `ec294b7` cross-fence dedup refcount before pack expiry; `ce9d521` migration 0045 needs expand-contract or a Recreate note; `3e0832b` measure the pack-leg mutex serialization; `022aec8` sweep append-after-abort hardening.\n\nKnown conscious trade-offs: old-wsfetch skew packs whole workspaces durable (documented at `DURABILITY_HEADER`; bounded by the reclaimer ticket); ADR-0067 carries dated implementation notes where the build refined the text (single-member packs for oversized members; Postgres rows as the operational read path).\n\n\n- `just test`: 992 passed / 28 skipped (env-gated live tiers + benchmarks), real Postgres + real MinIO.\n- Not live-verified on a cluster; the `just up` / `just local-helm` / kind wiring was audited (env flow, migration ordering) but not executed.\n- Before merging: `just pr-gate \u003cn\u003e`.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\n\n---\n\n\n**@thulasi-ram** — 2026-08-24T18:30:50Z\n\nPost-review additions on this branch, all suite-green (993/993 + UI typecheck) and independently judge-verified:\n\n- **8e11b01** fixes git-bug `8de85a8` (dogfood `version` step failed with class Step and no Pod): a bash *comment* containing `${{ outputs.version.tag }}` was textually interpolated against a needs-only context; the pre-Pod fail-fast now classifies `Config` instead of the lying `Step`. Follow-up ticket `4c2d1f6` = compile-time check for out-of-needs output references.\n- **2343e82** fixes git-bug `1344d1d` (Browse served a symlink as a file): listing gains `kind:\"symlink\"` + additive `target`, the stream is flagged `X-Scarab-Symlink: 1`, walk still never follows.\n- Ticket triage sweep ran alongside: **72 → 41 open**, 31 closed with independently verified evidence, 1 folded into `0ec3b39` (Export deletion).\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}