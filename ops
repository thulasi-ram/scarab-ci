{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":1,"timestamp":1787774713,"nonce":"L8kK7r7nM1M+y3A31G+9hMXAqAA=","title":"ci(dogfood): Scarab builds Scarab — .scarab/ci.yaml","message":"\u003e Imported from pull request #21 (MERGED)\n\u003e Branch: `dogfood/scarab-ci-pipeline` → `main`\n\u003e Created: 2026-07-17T15:24:21Z · Merged: 2026-07-17T16:30:18Z\n\nScarab's first self-hosted CI pipeline: a `clone` step provisions this repo at the run's pinned SHA, then a `rust:1-bookworm` step type-checks `scarab-identity` and publishes two artifacts of record. Also narrows the `/.scarab` gitignore so committed pipeline configs (`.scarab/*.yaml`, `.scarab/lib/`) are tracked while the local object store stays ignored.\n\nProven end-to-end: webhook ingest + HMAC + replay guard → installation-token minting → **private-repo clone** (credential-free `.git/config`) → workspace CAS handoff → `cargo check -p scarab-identity` **green** → `run_as_root` honored under the hardened baseline → durable wall-clock deadline enforcement (survived a control-plane suspend).\n\nRun `68af6899` succeeded (both steps green). Authoring iterations captured real edges: `bash -c` not `-lc` (login shell drops the rust PATH); cargo target → `/tmp` and git `safe.directory` because the CAS workspace is 65532-owned and the baseline drops `CAP_DAC_OVERRIDE`.\n\n- `98ea804` artifacts harvested + uploaded but never indexed (API `[]`) — harvest-annotation/scheduler race\n- `ba921db` `set_status` failures are silent (stuck outbox, no log)\n- `c653742` log-tail warn-spam during `PodInitializing`\n- `b04697f` `run_as_root` can't write the CAS-restored workspace (DAC_OVERRIDE dropped)\n- `03ef8e4` `/.scarab` gitignore blocked the pipeline path (fixed here)\n- `70aa42e` App-config runbook: must subscribe to Push/PR events + grant `statuses:write` (both fail silently)\n\nThe App currently has `events: []` and lacks `statuses:write`, so runs were triggered via hand-signed synthetic deliveries and no commit status posts yet. Once the App subscribes to Push/Pull request and is granted Commit statuses: write, opening/pushing this PR will drive the loop for real.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","files":null}]}