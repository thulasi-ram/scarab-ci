{"author":{"id":"2eecc7d99b502c158aacb0868dca9358610edc02724d21fe49b0196402d3364e"},"ops":[{"type":3,"timestamp":1785565393,"nonce":"siAXoH7gu5G1bDAf8SRQxCE2ers=","message":"**Done at `63e3c0b` + `e30f594` — by none of the ticket's three options.** The sweep already pages the FULL cold listing to find deletion candidates, so the detector is a set diff at zero added requests: residue = `marked − cold_listing − walk_missing`, alarmed per object with its address and the FIRST root that reached it (provenance recorded on first insertion, so the shared-subtree short-circuit and walk cost are unchanged). Unlike a cold-only walk this also detects missing BLOBS — the walk never reads blob bodies, so options (a)/(b) structurally could not, and (a) would have been destructive under ADR-0064 (a pre-flush root reads dangling → forget → sweep). Suppression: `gc_workspace_roots` now returns `(root, MIN(recorded_at))`; residue under a root younger than the existing grace window is counted but not alarmed (a flush may be in flight). Two gauge-style metrics, stored per pass so recovery shows as return-to-zero. The alarm text distinguishes \"warm still holds it — re-uploadable\" from \"not readable from warm either — total loss\" (blob residue is warm-probed within the 50-entry log cap). Cost stated per the ticket: +0 requests, ~MBs of pass memory, first-root attribution, one-pass detection latency. Detection only — re-heal from warm is deliberately a follow-up; gauges-stale-on-leadership-change filed as 231040a.","files":null}]}