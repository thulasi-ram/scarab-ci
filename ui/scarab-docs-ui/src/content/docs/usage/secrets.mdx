---
title: Secrets
description: Named secrets, encrypted with an envelope scheme and never readable back — scoped at org, repo, and environment with env → repo → org inheritance, resolved at the point where they are used.
---

import Doodle from '../../../components/Doodle.astro';

<Doodle icon="key-round" size={200} rotate={9} opacity={0.18} top="7rem" right="-2rem" />

## What a secret is

A secret is a named credential Scarab stores on your behalf and hands to a step at run time —
a registry password, a deploy token, an API key. You give it a **name** (the key a step
references) and a **value**; Scarab owns storage, scoping, injection, and the one thing everyone
gets wrong: keeping a run from an untrusted fork's pull request from ever seeing it.

## Encrypted at rest, write-only

Values are **encrypted with an envelope scheme in Postgres** (ADR-0014): a per-tenant data key encrypts the
value, and that key is wrapped by a master **KEK** — the base64 32-byte `SCARAB_MASTER_KEY`
(required in production; see [Environment Variables](/scarab-ci/usage/environment-variables/)).
Storage sits behind a pluggable `SecretProvider` port, so Vault, a cloud secret manager, or
the External-Secrets-Operator can drop in later without touching pipelines.

A secret value can be **written but never read back**. No API ever returns it — `POST /v1/secrets` takes the value,
but `GET /v1/secrets` lists **names only**, never values. In the UI the copy is exact:

> Encrypted at rest, never displayed — overwrite but never read back.

You can replace a value (the create form guards an overwrite of an existing name behind an
explicit opt-in), but you can never read the current one back. Log redaction does its best to mask
known secret values out of the live and stored log stream.

![Creating a secret in the Scarab UI](../../../assets/secrets.jpg)

## Scoping and inheritance

Secrets are scoped at three levels — **org → repo → environment** — and a step resolves a key by
walking that chain so that the **most specific scope wins: `env → repo → org`** (ADR-0037). A value shared across
environments lives **once** at repo or org scope; only values that genuinely differ are stored
per-environment. The scope is chosen on write with `org` (required), an optional `repo`, and an
optional `environment` (which requires `repo`).

A pipeline references a key **without naming an environment** — resolution picks the value by the run's
target [environment](/scarab-ci/usage/environments/), so there is no `_staging` / `_prod` naming
convention baked into the YAML, and pipelines stay portable across environments.

## How a pipeline consumes them

A step names the keys it needs with `secrets: [KEY, ...]`; each is resolved against the run's scope
chain and made available to that step under the same name. A key that resolves to nothing at
**every** scope is an error at submit or deploy time, never a silent empty. A **run from a fork's
pull request is locked out** and receives none unless a maintainer approves. For the authoring YAML see
[Pipeline Syntax](/scarab-ci/usage/pipeline-syntax/).

## The secret-coverage view

The UI carries an advisory **secret matrix** (`GET /v1/repos/{org}/{repo}/secrets/matrix`): for
each key, its **effective** status per environment after inheritance — `set` (defined at that
env's scope), `inherited` (resolves from repo/org scope), or `unset`. It is names and status only,
never values, and it never blocks a deploy — the completeness check that actually matters is the one
driven by usage at submit or deploy time.

## References

- [ADR-0014 — Secrets: envelope-encrypted Postgres + pluggable providers](/scarab-ci/tech/adr/0014-secrets/)
- [ADR-0037 — Environment governance: scoped secrets, approval-as-gate, admission at point-of-use](/scarab-ci/tech/adr/0037-environment-governance/)
