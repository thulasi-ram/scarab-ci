---
title: Scarab
description: A modern CI engine for Kubernetes. It gives you the cohesion of a CI that lives in your forge, built on a control plane that treats a run as state rather than a process. Written in Rust.
template: splash
hero:
  layout: split-left
  tagline: A modern CI engine for Kubernetes that lives in your forge. It treats your run as state, not a process you hope stays alive.
  image:
    # The brand emblem, square-cropped (1:1) — ink outline on light, gold on dark.
    light: ../../assets/brand/scarab-emblem-square.svg
    dark: ../../assets/brand/scarab-emblem-dark-square.svg
    alt: 'The Scarab emblem: a scarab with spread wings inside a ring'
  actions:
    - text: Run it locally
      link: /scarab-ci/get-started/run-locally/
      icon: right-arrow
    - text: Why an engine, not a runner
      link: /scarab-ci/tech/adr/0001-ci-as-durable-execution/
      icon: open-book
      variant: minimal
---

import AsciiScene from '../../components/AsciiScene.astro';

CI tools tend to fall into two camps. **Workflow engines** (Argo, Tekton, Temporal) have a
durable core that survives a crash, but they aren't really CI: forge integration, in-repo config,
PR checks, identity, secrets, the DSL, and the UI are all yours to build. **CIs that live in your
forge** (GitHub Actions, GitLab, Woodpecker, Drone) hand you all of that, but underneath, the
orchestrator is just a job runner. If the control plane restarts partway through a run, the run is
orphaned or starts over. Scarab is built to be **both at once**: the batteries-included feel of a
forge CI, sitting on a control plane that treats a run as durable **state** rather than a process
it forgets the moment it restarts.

![The Scarab dashboard: an action inbox of runs paused on a gate, recently visited repos shown as pass/fail status cards, and the repo list sorted by what you touched last](../../assets/dashboard.jpg)

## What makes it different

<figure class="sc-home-camps" role="img" aria-label="Workflow engines have a durable core but are not CI; forge CIs are batteries-included but not durable. Scarab is both at once.">
  <svg viewBox="0 0 640 224" xmlns="http://www.w3.org/2000/svg" class="sc-home-camps-svg">
    <defs>
      <marker id="sc-home-chev" viewBox="0 0 8 8" refX="4" refY="6.4" markerWidth="7" markerHeight="7" orient="auto">
        <path d="M1.4 3 L4 6 L6.6 3" class="sc-home-chev-head" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
      </marker>
    </defs>
    <g class="sc-home-camp">
      <rect x="34" y="20" width="214" height="62" rx="12" class="sc-home-camp-box"/>
      <text x="141" y="47" class="sc-home-camp-title" text-anchor="middle">workflow engines</text>
      <text x="141" y="66" class="sc-home-camp-sub" text-anchor="middle">durable core &middot; not CI</text>
    </g>
    <g class="sc-home-camp">
      <rect x="392" y="20" width="214" height="62" rx="12" class="sc-home-camp-box"/>
      <text x="499" y="47" class="sc-home-camp-title" text-anchor="middle">forge CIs</text>
      <text x="499" y="66" class="sc-home-camp-sub" text-anchor="middle">batteries &middot; not durable</text>
    </g>
    <path d="M141 82 C141 122 282 110 282 148" class="sc-home-flow" fill="none" marker-end="url(#sc-home-chev)"/>
    <path d="M499 82 C499 122 358 110 358 148" class="sc-home-flow" fill="none" marker-end="url(#sc-home-chev)"/>
    <rect x="230" y="150" width="180" height="56" rx="13" class="sc-home-both-box"/>
    <text x="320" y="176" class="sc-home-both-title" text-anchor="middle">Scarab</text>
    <text x="320" y="193" class="sc-home-both-sub" text-anchor="middle">both at once</text>
  </svg>
</figure>

<dl class="sc-home-specsheet">
  <div class="sc-home-spec-row">
    <dt class="sc-home-spec-key">cohesion</dt>
    <dd class="sc-home-spec-body">
      <p class="sc-home-spec-claim">One product, not a kit.</p>
      <p class="sc-home-spec-support">A CI that lives in your forge (DSL, secrets, approvals, identity, UI) on a durable engine. Not a workflow engine you have to build a CI on top of, and not a job-runner CI with nothing durable underneath.</p>
    </dd>
  </div>
  <div class="sc-home-spec-row">
    <dt class="sc-home-spec-key">durability</dt>
    <dd class="sc-home-spec-body">
      <p class="sc-home-spec-claim">Runs are state, not processes.</p>
      <p class="sc-home-spec-support">A durable state machine on Postgres, in the spirit of DBOS. When the control plane restarts, a run resumes from its last completed step, and each step runs exactly once. That's the architecture doing its job, not a boast.</p>
    </dd>
  </div>
  <div class="sc-home-spec-row">
    <dt class="sc-home-spec-key">identity</dt>
    <dd class="sc-home-spec-body">
      <p class="sc-home-spec-claim">Keyless by design.</p>
      <p class="sc-home-spec-support">OIDC that doesn't care which forge you use, with Scarab acting as its own OIDC issuer so runs can federate into your cloud without long-lived keys. Adapters for GitHub and Forgejo are implemented today. See Status.</p>
    </dd>
  </div>
  <div class="sc-home-spec-row">
    <dt class="sc-home-spec-key">the DSL</dt>
    <dd class="sc-home-spec-body">
      <p class="sc-home-spec-claim">A real language, not templated YAML.</p>
      <p class="sc-home-spec-support">A typed intermediate representation with a YAML frontend and CEL expressions. Under it all is one flat recursive DAG, where <code>invoke</code> is reuse and matrix is just a modifier.</p>
    </dd>
  </div>
</dl>

## Why now

<AsciiScene name="dungroller" fontSize={5} class="ascii-accent" />

As pipelines get longer and more autonomous (agents, review gates that sit open for hours,
workflows that span days), "the job died, click re-run" stops being good enough. Scarab can hold
a paused run almost for free and pick up exactly where it left off, and it keeps an event log that
only ever grows, so you can see everything that happened. That's where a durable core earns its
keep. It's also the kind of foundation that agent-driven development actually wants, rather than
just a nicer YAML dialect. And Scarab is *built* that way too: ambitious systems software from a
small team, at a pace that used to take a room full of people.

![A Scarab run: the step DAG, live logs, and an event timeline showing a retried step and the run paused on a manual deploy gate](../../assets/run-detail.jpg)

## Lineage

Scarab owes its shape to [Woodpecker](https://woodpecker-ci.org/): lean CI that lives in your
forge, with none of the enterprise ceremony. It's shaped just as much by Woodpecker's *limits*:
the many backends it has to support, and the pace a volunteer project can sustain. Sticking to
Kubernetes alone is us shedding that backend baggage on purpose, and the pace is what a small team
leaning on AI can now hold.

## Where to go next

<div class="sc-home-next">
  <a class="sc-home-next-el" href="/scarab-ci/get-started/run-locally/">
    <span class="sc-home-next-main">
      <span class="sc-home-next-label">Run it locally</span>
      <span class="sc-home-next-desc">bring up Postgres + MinIO + a kind cluster and watch a pipeline complete</span>
    </span>
    <span class="sc-home-next-arrow" aria-hidden="true">&rarr;</span>
  </a>
  <a class="sc-home-next-el" href="/scarab-ci/tech/context/">
    <span class="sc-home-next-main">
      <span class="sc-home-next-label">Read the design</span>
      <span class="sc-home-next-desc">the thesis, the durability contract, and the ubiquitous language: 40+ ADRs on the why</span>
    </span>
    <span class="sc-home-next-arrow" aria-hidden="true">&rarr;</span>
  </a>
</div>

:::note[Status: honest]
A proven core, with real forge and Kubernetes I/O wired in around it. **Proven against real
Postgres:** the durable engine, crash and resume, exactly-once execution, restarting a single
step, gates, the content-addressed workspace, `invoke`, secrets, and the OIDC issuer (the
workspace suite passes locally). **Implemented:** the GitHub adapter (it ingests webhooks and
posts commit statuses) and a Forgejo adapter, so more than one forge is supported; Pod execution
on a live Kubernetes cluster (`scarab-executor-k8s`, with cluster tests); the results-egress
sidecar image (`docker/sidecar/`); and CI that runs the Rust suite on every push
(`.github/workflows/ci.yml`). None of it is yet hardened at scale. Read this as a proven state
machine with its edges freshly wired up: implemented, but not yet battle-tested in production.
:::

## References

- [ADR-0005: Tenancy & deployment: Kubernetes as the only backend](/scarab-ci/tech/adr/0005-tenancy-and-k8s-only/)

<style>{`
/* ===== Home-page scoped styles (sc-home-*) — do not leak to other pages ===== */

/* ---- Positioning diagram ---- */
.sc-home-camps { margin: 0 0 2.5rem; padding: 0; }
.sc-home-camps-svg {
  width: 100%;
  height: auto;
  display: block;
  max-width: 560px;
  margin-inline: auto;
}
.sc-home-camp-box, .sc-home-both-box {
  fill: color-mix(in oklab, var(--scarab-emerald) 4%, transparent);
  stroke-width: 1.25;
  stroke-dasharray: 1.5 4;
  stroke-linecap: round;
}
.sc-home-camp-box { stroke: color-mix(in oklab, var(--muted-foreground, gray) 55%, transparent); }
.sc-home-both-box {
  stroke: var(--scarab-copper);
  stroke-dasharray: 2 4;
  fill: color-mix(in oklab, var(--scarab-copper) 9%, transparent);
}
.sc-home-camp-title, .sc-home-both-title {
  font-family: var(--sl-font-mono);
  font-size: 14px;
  font-weight: 500;
  fill: var(--foreground, var(--sl-color-white));
}
.sc-home-both-title { fill: var(--scarab-copper); font-size: 15px; }
.sc-home-camp-sub, .sc-home-both-sub {
  font-family: var(--sl-font-mono);
  font-size: 10.5px;
  fill: var(--muted-foreground, var(--sl-color-gray-3));
}
.sc-home-both-sub { fill: color-mix(in oklab, var(--scarab-copper) 80%, var(--muted-foreground)); }
.sc-home-flow {
  stroke: var(--scarab-emerald);
  stroke-width: 1.5;
  stroke-dasharray: 1.5 5;
  stroke-linecap: round;
  opacity: 0.9;
}
.sc-home-chev-head {
  stroke: var(--scarab-emerald);
  stroke-width: 1.4;
}

/* ---- Spec-sheet definition list ---- */
.sc-home-specsheet { margin: 0 0 0.5rem; padding: 0; }
.sc-home-spec-row {
  display: grid;
  grid-template-columns: 10.5rem 1fr;
  gap: 1.5rem;
  padding: 1.15rem 0;
  border-top: 1px dotted color-mix(in oklab, var(--scarab-emerald) 26%, var(--border));
  align-items: baseline;
}
.sc-home-spec-row:last-child {
  border-bottom: 1px dotted color-mix(in oklab, var(--scarab-emerald) 26%, var(--border));
}
.sc-home-spec-key {
  font-family: var(--sl-font-mono);
  font-size: 0.9rem;
  font-weight: 500;
  color: var(--scarab-emerald);
  letter-spacing: 0.01em;
  position: relative;
  padding-left: 0.9rem;
}
.sc-home-spec-key::before {
  content: "\\2022";
  position: absolute;
  left: 0;
  color: color-mix(in oklab, var(--scarab-emerald) 60%, transparent);
}
.sc-home-spec-body { margin: 0; }
.sc-home-spec-claim {
  margin: 0 0 0.35rem;
  font-weight: 700;
  font-size: 1.05rem;
  line-height: 1.35;
  color: var(--foreground, var(--sl-color-white));
}
.sc-home-spec-support {
  margin: 0;
  font-size: 0.92rem;
  line-height: 1.6;
  color: var(--muted-foreground, var(--sl-color-gray-3));
  max-width: 46rem;
}

/* ---- Editorial "next" links ---- */
.sc-home-next { margin: 0; }
.sc-home-next-el {
  display: flex;
  align-items: center;
  gap: 1.5rem;
  padding: 1.3rem 0.25rem 1.3rem 0;
  text-decoration: none !important;
  border-top: 1px dotted color-mix(in oklab, var(--scarab-emerald) 26%, var(--border));
  color: inherit;
  transition: padding-left 0.18s ease, background 0.18s ease;
}
.sc-home-next-el:last-child {
  border-bottom: 1px dotted color-mix(in oklab, var(--scarab-emerald) 26%, var(--border));
}
.sc-home-next-el:hover { padding-left: 0.75rem; }
.sc-home-next-main { display: flex; flex-direction: column; gap: 0.3rem; flex: 1; }
.sc-home-next-label {
  font-family: var(--sl-font-mono);
  font-size: 1.25rem;
  font-weight: 500;
  color: var(--foreground, var(--sl-color-white));
  letter-spacing: -0.01em;
}
.sc-home-next-el:hover .sc-home-next-label { color: var(--scarab-emerald); }
.sc-home-next-desc {
  font-size: 0.9rem;
  color: var(--muted-foreground, var(--sl-color-gray-3));
  line-height: 1.5;
}
.sc-home-next-arrow {
  font-family: var(--sl-font-mono);
  font-size: 1.6rem;
  color: var(--scarab-emerald);
  transition: transform 0.18s ease;
  flex-shrink: 0;
}
.sc-home-next-el:hover .sc-home-next-arrow { transform: translateX(4px); }

/* ---- Responsive ---- */
@media (max-width: 640px) {
  .sc-home-spec-row { grid-template-columns: 1fr; gap: 0.5rem; }
  .sc-home-next-el { gap: 0.75rem; }
  .sc-home-next-label { font-size: 1.05rem; }
}

/* ---- Reduced motion: no essential motion; disable hover transitions ---- */
@media (prefers-reduced-motion: reduce) {
  .sc-home-next-el, .sc-home-next-arrow { transition: none; }
}
`}</style>
