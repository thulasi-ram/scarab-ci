# direnv: export `.env.local` into the shell on `cd` (run `direnv allow` once).
# `.env.local` holds machine-specific dev config; copy it from
# `.env.local.example`. Committed values are safe defaults only.
dotenv_if_exists .env.local
